[build]
builder = "RAILPACK"

[deploy]
numReplicas = 1
startCommand = "uv run opinions-agent init-runtime && uv run opinions-agent serve"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
overlapSeconds = 0
drainingSeconds = 30
