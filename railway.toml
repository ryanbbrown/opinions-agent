[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run opinions-agent serve"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "opinions-agent"
