# Eval Optimization Experiments

Append-only ledger of prompt-optimization experiments, owned by the driver in the **main checkout** — worktree copies are disposable and invisible to each other, so results must be recorded here. One entry per experiment, newest at the bottom. See `eval/GOAL.md` for the protocol. Braintrust (project `opinions-agent`) holds the full scores and traces; this file is the narrative history so the loop can build on prior results without a round-trip.

Cost note: do not budget off Braintrust's `estimated_cost` — it prices every input token at full rate and ignores OpenAI's automatic prompt caching (thinharness discards the `cached_tokens` detail), so it runs ~1.8x high. A full 9-week run is ~$5, not ~$10.

**Current best:** `exp/keep-the-list` (coverage-granularity + fidelity rules for full enumerations and explicit corrections of default assumptions)

**Score to beat:** 0.615 mean `opinion_quality` (pooled replicates: 0.487 `keep-the-list`, 0.698 `-run2`, 0.660 `-run3`; spread 0.21 — run 1 is a low outlier). On promotion, update this to the new best's replicate mean.

## Entry format

Each entry records:

- **Experiment** — matches the Braintrust `--experiment` name.
- **Hypothesis** — the change and why it should help.
- **Changed** — one-line summary of the `prompts.py` / `RULES.md` edit.
- **Scores** — mean `opinion_quality` (primary), `evidence_recall`, `evidence_precision`.
- **Verdict** — promoted or reverted, and why.
- **Learned** — anything that should steer the next experiment.

---

### baseline

- **Hypothesis:** Establish the unmodified-prompt scores that every variant must beat.
- **Changed:** none (current `prompts.py` and `RULES.md`).
- **Scores:** opinion_quality 0.358, evidence_recall 0.877, evidence_precision 0.575 (fresh 9-week run under the current binary judge). A prior run's outputs re-judged by the same judge (`binary-judge-rescore`) scored 0.390 / 0.939 / 0.589 — a ~3–6pt run-to-run spread on the same prompt, which is the noise floor variants must clear.
- **Verdict:** current best by default.
- **Learned:** Agent runs are stochastic (proposal count and which evidence converts vary per run); a single run's aggregate is noisy to ±3–6pt.

### fidelity-check

- **Hypothesis:** The dominant `opinion_quality` failure is the agent preserving a stance but dropping a load-bearing piece of the canonical claim. Operationalizing fidelity with a concrete pre-send check should lift quality where concepts were dropped.
- **Changed:** `RULES.md` "Opinion Writing Fidelity" — added a pre-send check that names the four categories most often lost (co-equal second half of a two-part claim; bounding conditions/caveats; named example/term/mechanism/number; the evidence's actual center of gravity vs a substituted thesis). `prompts.py` unchanged.
- **Scores:** opinion_quality 0.308 (−0.050), evidence_recall 0.849 (−0.028), evidence_precision 0.557 (−0.018). Single run.
- **Verdict:** Not promoted. Net-negative on a single run.
- **Learned:** The change has real directional signal — all 4 fail→pass flips (W04-01, W08-01, W08-03, W11-04) are exactly the concept-drop failures it targets. But 6 previously-passing targets flipped to fail (W04-03, W04-04, W05-02, W05-03, W11-03, W13-02), for net −2. With 10 targets flipping in one run and noise at ±3–6pt, a single run cannot separate the 4 real gains from noise churn — this is the concrete case for the multi-run + replicate rule. If revisited: run 2–3x to see whether the 4 gains replicate and the 6 losses are stable or noise, and test a leaner phrasing (the check added ~8 lines) to check whether a tighter version keeps the gains without the churn.

### argument-shape

- **Hypothesis:** The leaner fidelity rewrite suggested by fidelity-check's learned note: replace (not append to) the abstract fidelity section with a "compressed argument" shape — keep the mechanism, the named anchors, the bounds, and both halves of a compound claim — and remove the abstraction pull from "prefer stances over summaries", so concept-drop failures convert without checklist churn.
- **Changed:** RULES.md "Opinion Writing Fidelity" rewritten as the four-part argument shape (mechanism / concrete anchors / bounds / whole compound, with a non-test example); stance bullet gains "taking a side, not abstracting"; prompts.py's duplicate post-read fidelity paragraph consolidated into a pointer at the rules.
- **Scores:** opinion_quality 0.365 (−0.009 vs pooled baseline 0.374), evidence_recall 0.913 (+0.005), evidence_precision 0.573 (−0.009). Single screening run; below the score to beat, so stopped per protocol.
- **Verdict:** Not promoted. Flat — inside the noise band.
- **Learned:** From per-target diffs plus reading the run's actual proposals in the worktree `.runs` DBs: (a) anchor-keeping guidance does move the named-anchor failure class — two stable-fail targets flipped to pass (W08-04, W13-04) and held gains on W04-02/W05-03; (b) unmatched targets rose 3→5 — the W13 trace shows two same-theme arguments from different documents merged into one proposal, so one target consumed the match and the other auto-zeroed; the "whole compound" rule needs an explicit one-argument scope plus an anti-merge granularity rule; (c) the agent systematically skips reflective/personal-register targets (career growth, sustainable ambition, work-satisfaction classes) across weeks — the negative eligibility filters ("obvious practices", "generic career advice") suppress a class the canon actually converts; (d) one causal claim was inverted into a how-to prescription and failed for losing the claim; (e) skips + merge collisions ≈ 0.12–0.15 of mean headroom, the largest quantified lever visible in traces — bigger than remaining wording-fidelity churn.

### coverage-granularity

- **Hypothesis:** Attack the quantified coverage levers from the argument-shape trace review in one coherent redesign: recalibrate eligibility (filter reference material, not topics; personal/reflective stances are first-class; causal claims are stances — don't invert them into recipes), enforce one proposal per argument (anti-merge, with cross-document synthesis only for the same claim), propose revisions when evidence extends an existing opinion, and keep the argument-shape fidelity core (mechanism/anchors/bounds/compound) with an explicit one-argument scope.
- **Changed:** RULES.md — rewrote four Generation Rules bullets ("Avoid empty consensus, not familiar claims", "Filter reference material, not topics", "One proposal per argument", "Extend existing opinions instead of skipping near-duplicates"), strengthened the career/work-life bullet, updated Practical Proposal Test item 2, and replaced the fidelity section with the scoped argument-shape version; prompts.py — narrowed the triage filter ("generic career advice" → "step-by-step tactics without an argued stance") and added revision-spotting to the OPINIONS.md read instruction.
- **Scores:** opinion_quality **0.483 pooled** (runs 0.485 / 0.454 / 0.508, spread 0.054) vs 0.374 baseline (+0.109, outside the ±3–6pt noise band). evidence_recall 0.932 (+0.024). evidence_precision 0.536 (−0.046, above the 0.50 floor).
- **Verdict:** **Promoted.** First run cleared the score to beat, both replicates confirmed; mean beats baseline by ~2× the noise band. Precision cost is real but within the guardrail.
- **Learned:** (a) The coverage levers paid off exactly as quantified: previously-skipped personal-register targets started converting (W06-04 2/3, W08-02 2/3, W10-02 2/3, W11-05 and W12-03 1/3 each), and W04-01/W04-02 improved — 13 targets moved up vs 8 down, with the ups mostly stable-fail→passing and the downs mostly churn among flaky targets; (b) anchor gains from argument-shape held (W08-04 and W13-04 now 3/3); (c) remaining stable fails (0/3) cluster in two groups: stubborn anchor/half-drops (W04-03 specialization-mechanisms list, W04-05 delivery-practice half, W05-04 not-only-income half, W06-01 Figma-position framing, W08-01 floor-not-result, W11-04 "operational scars", W12-01 ownership-as-distinct, W13-01 Shortcut example) and enumerated-list drops (W13-03 named feedback sources); W13-05 still never converts — the revise-existing rule did not produce a revision proposal for it; (d) W11-02 went unmatched 1/3 — watch for residual merging in W11; (e) precision drift (−0.046) is the cost of broader eligibility; further eligibility loosening has little headroom left before the 0.50 floor.

### opus-4-8-medium

- **Hypothesis:** User-requested model experiment (not a prompt variant): run the current-best prompts (`exp/coverage-granularity`) on `anthropic:claude-opus-4-8` with adaptive thinking and effort medium, instead of `openai:gpt-5.5` medium.
- **Changed:** `config.py` model → `anthropic:claude-opus-4-8`; `agent.py` — provider-aware extra_body (`thinking: adaptive` + `output_config.effort` for Anthropic; the OpenAI-style `reasoning` dict would 400), structured output via `output_mode="auto"` (Anthropic resolves to tool mode; OpenAI stays native), explicit `AnthropicMessagesModel` with `max_tokens=16000` (thinharness defaults Anthropic to 1024), request timeout 600s. Prompts/RULES.md unchanged from `exp/coverage-granularity`.
- **Scores:** opinion_quality 0.475 (vs 0.483 pooled current best — a wash on one run), evidence_recall 0.922, evidence_precision **0.627** (+0.091 over current best — the best precision of any run so far). ~2.9 tool calls/week vs ~6.4 (reads sources far less), wall-clock ~12 min vs ~5 min per 9-week run.
- **Verdict:** Not promoted within the prompt loop (single run, below the 0.483 score to beat; a model swap is also outside the prompt-lever edit scope, so adopting it is the user's call). Branch `exp/opus-4-8-medium` kept for reference.
- **Learned:** Model choice changes the failure *profile* more than the mean: Opus flipped several stubborn stable-fails to pass (W06-01 Figma framing, W13-05 revise-existing, W04-01, W05-01, W10-02, W11-05) but dropped targets gpt-5.5 passes reliably (W04-02 one-way ratchet 3/3→0/1, W13-04 3/3→0/1, W05-03, W08-02). Its precision (+0.09) suggests tighter evidence triage with far fewer source reads. If the user wants to pursue Opus, replicates plus effort=high would be the natural next probe; an ensemble insight — some targets seem model-dependent — also hints remaining prompt headroom is real on either model.

### keep-the-list

- **Hypothesis:** The two dominant remaining failure clusters after coverage-granularity are enumerated members compressed away (the "tools, data, skills, and harnesses" class) and corrections-of-a-default dropped (the "goal is NOT maximum delegation" class). Adding two targeted fidelity bullets — keep the full enumeration by name; state the correction, not just the positive replacement — should convert those without new churn.
- **Changed:** RULES.md fidelity section only — added "The full enumeration" bullet and rewrote "The bounds" into "The bounds and corrections" (net +1 bullet). Branched from `exp/coverage-granularity`.
- **Scores:** opinion_quality **0.615 pooled** (runs 0.487 / 0.698 / 0.660, spread 0.210) vs 0.483 current best (+0.133). evidence_recall 0.967 (+0.035; run2 hit 1.000). evidence_precision 0.532 (−0.004, above the 0.50 floor).
- **Verdict:** **Promoted.** Mean beats the score to beat by ~2–4× the historical noise band even counting the low run-1 outlier; both replicates individually landed 0.66+. Consistency is the concern: 0.21 spread is the widest of any variant, driven by run 1 (0.487) — runs 2–3 agree closely.
- **Learned:** (a) The enumeration/correction bullets converted their target classes: W11 became near-perfect (W11-01/02/03/05 3/3, W11-04 "operational scars" 2/3), W12-01 2/3, W13-03 2/3, W13-05 2/3 (first conversions ever for the last three), plus W05-01/W06-04/W08-02 3/3; (b) net +13 targets up / 4 down vs coverage-granularity, and the downs (W04-02, W05-02, W08-04, W10-02) are anchor-class targets that were previously stable — watch whether the longer fidelity list is starting to crowd out the "concrete anchors" bullet; (c) run-to-run spread grew with the rule count — a future leanness pass (consolidating the six fidelity bullets) is a candidate simplification experiment; (d) remaining stable fails are W04-03 (still drops the specialization-mechanisms list) and W04-05/W06-01/W13-01 (named-example class), giving a concrete next hypothesis: the enumeration rule works for claim-member lists but not for lists that live inside a *named example*.

### lean-reads

- **Hypothesis:** User-directed experiment: the current best prompts (`exp/keep-the-list`) plus main's read-efficiency changes — targeted `jsonl_search` by opinion_id on OPINIONS_SOURCES.jsonl instead of a wholesale read, the decision log made write-only (never read), and explicit row schemas for both artifacts in the prompt — should keep quality while cutting wasted reading.
- **Changed:** Cherry-picked main's efficiency commit (`5295255`) onto `exp/keep-the-list`; the one conflict (the read-instructions paragraph in `prompts.py`) was resolved by keeping keep-the-list's revision-spotting clause on the OPINIONS.md read plus main's targeted-sources policy and decisions-read removal. RULES.md unchanged from keep-the-list.
- **Scores:** opinion_quality 0.548 (vs 0.615 score to beat; inside keep-the-list's 0.487–0.698 replicate range), evidence_recall 1.000, evidence_precision 0.508 (just above the 0.50 floor). Single run per user instruction. Efficiency: prompt_tokens/wk 92.6k vs keep-the-list's 97–125k (~13% below the pooled mean), est cost $1.11/wk vs $1.15–1.41; tool_calls/wk essentially unchanged (6.78 vs 6.56–6.89).
- **Verdict:** Not promoted (single run below the score to beat), but no evidence the efficiency changes hurt quality — the run lands mid-range of keep-the-list's own replicates, and the per-target diff is churn-shaped (10 up / 10 down) rather than a systematic regression.
- **Learned:** (a) The token savings are real but modest — the wholesale sources/decisions reads were only ~5–25k prompt tokens/wk; tool-call count didn't drop because the agent still does the same triage reads; (b) this run's downs cluster in one bad week (all four W08 targets 0/1, plus W06-04 and W11-05 which keep-the-list passed 3/3) while W10-02/W10-03/W13-03/W13-05 all flipped up — replicates would be needed before reading anything target-level into the read-policy change; (c) precision at 0.508 is the closest any run has come to the 0.50 floor — watch it if this branch becomes the base for future variants.

### judge-variance probe (diagnostic, not a prompt experiment)

- **Question:** How much of the run-to-run `opinion_quality` spread (keep-the-list: 0.487/0.698/0.660) is judge inconsistency rather than proposal variance?
- **Method:** (a) Re-judged identical stored outputs with the proxy cache disabled (`x-bt-use-cache: never` — note `eval rescore` alone is useless for this: the Braintrust proxy caches temperature-0 judge calls, so a plain rescore returns bit-identical verdicts). keep-the-list run 1 judged 3x, run 2 judged 3x. (b) For the 17 targets whose verdicts flip across keep-the-list runs 1–3, compared the matched proposal texts (difflib similarity) to attribute each flip.
- **Findings:** (a) Judge noise on identical outputs is real but small: run 1 scored 0.4875 all 3 times (0 flips, matches the stored 0.487); run 2 scored 0.635/0.635/0.667 vs 0.698 at run time — a ~3–6pt session-level drift from 1–2 borderline verdicts per judging (W06-03 was one). (b) Of the 17 cross-run flip targets, 16 had materially different matched proposal texts (sim 0.01–0.56) → proposal variance; 1 (W05-03, sim 1.0, pass/pass/unmatched) was a greedy-matching artifact, not a judge flip.
- **Implications:** The 0.21 keep-the-list spread is dominated by proposal variance, not the judge; the protocol's ±3–6pt noise band is exactly the judge+matching floor and stays valid. Borderline proposals are where the judge wavers — decisive proposals judge deterministically. Comparing runs judged on different days carries up to ~6pt of judge-session drift, so same-day replicates are the cleanest comparisons; a variant's gain must clear the band regardless. Fixing the variance means stabilizing the agent's proposals (e.g., the planned fidelity-leanness pass), not the judge.

### anchor-examples

- **Hypothesis:** The four never-passing named-example targets (W04-03, W04-05, W06-01, W13-01) fail because the agent states the abstracted lesson and drops the named case the argument reasons through, or paraphrases the source's own category terms. Strengthening the "concrete anchors" bullet in place (no new bullet, to avoid crowding) — illustrative cases are part of the claim; use the argument's own vocabulary — should convert them.
- **Changed:** RULES.md "The concrete anchors" bullet rewritten in place. Branched from `exp/lean-reads` (deviation from "branch from current best" flagged to the user: the read-efficiency changes are production behavior on main and were quality-neutral; score to beat stays 0.615).
- **Scores:** opinion_quality **0.623 pooled** (runs 0.683 / 0.508 / 0.677, spread 0.175) vs 0.615 — +0.008, inside the noise band. evidence_recall **1.000 pooled** (all three runs 1.000 — first variant with perfect recall). evidence_precision 0.5065 (−0.025 vs keep-the-list; 0.007 above the floor).
- **Verdict:** Not promoted. Screen passed (0.683) but the replicate mean is a wash; precision is drifting toward the 0.50 floor.
- **Learned:** (a) W04-05 converted (0/3→2/3) — the case/vocabulary guidance moves the paraphrase class; (b) reading the matched proposals for the three still-dead targets shows they are not simple name-drops: W04-03 consistently centers the *other half* of the source argument (generalist-vs-specialist) and omits the prescription half (where specialization should live); W06-01 now names the case in 2/3 runs but centers a different framing than the canon; W13-01 compresses the architecture well but never carries the anchoring example, and one run still paraphrased the source's key terms. Two consecutive fidelity-wording experiments have failed to move these three — the residual failure is argument-centering, and more bullet text shows diminishing returns; (c) counterbalancing regressions (W04-01 2/3→0/3, W08-01 1/3→0/3, W08-03, W13-04/05 down) look like proposal-variance churn per the judge-variance diagnostic; (d) consecutive non-promotions since keep-the-list: 2 (lean-reads, anchor-examples) — the next miss triggers the plateau stop.

### lean-fidelity

- **Hypothesis:** The judge-variance diagnostic attributed the run-to-run spread to proposal variance, and the ledger's standing suspicion was fidelity-bullet crowding. Consolidating the five fidelity bullets into three non-overlapping ones (mechanism / named specifics incl. cases, enumerations, vocabulary / whole claim incl. corrections, bounds, both halves) should stabilize proposals while preserving every concept class — promotable by beating 0.615 or by leanness-within-noise.
- **Changed:** RULES.md fidelity section only: five bullets merged into three, ~35% shorter, no concept class removed. Branched from `exp/lean-reads`.
- **Scores:** opinion_quality 0.446 (−0.169 vs 0.615 — far outside the noise band), evidence_recall 0.948, evidence_precision 0.495 (below the 0.50 floor). Single screening run, stopped per protocol.
- **Verdict:** Not promoted — fails both promotion paths (quality collapse, and leanness requires holding scores within noise).
- **Learned:** (a) The consolidation actively hurt: quality fell back toward coverage-granularity territory (0.483), as if the enumeration/correction gains that keep-the-list earned partially evaporated when those rules stopped being separate, explicit bullets. The five-bullet section's redundancy was load-bearing — one bullet per failure class appears to be what the agent actually follows; (b) future leanness passes on RULES.md should trim qualifiers and examples inside bullets, not merge rule categories; (c) this is the third consecutive non-promotion (lean-reads 0.548, anchor-examples 0.623 pooled, lean-fidelity 0.446) — **plateau stop condition met**; the loop ends with `exp/keep-the-list` (+ production efficiency changes = `exp/lean-reads`) as the final best at 0.615.

### opinion_attempted metric (eval infrastructure, not a prompt experiment)

- **What:** A second LLM judge alongside `opinion_quality`, landed on main (`f81a233`). `opinion_attempted` is the lenient layer of the funnel: a target counts as attempted when its matched proposal expresses the same central claim, even if it dropped supporting concepts, named examples, numbers, or caveats. A quality pass implies attempted, so the lenient judge only runs on matched-but-failed targets (~10-15 extra sonnet calls per run). Both judges share one match-and-judge pass per week, so matching is never duplicated and the two metrics can't disagree about which proposal a target matched. The metric triad now reads as a funnel: evidence_recall (citations used) → opinion_attempted (right claims proposed) → opinion_quality (right claims written completely); fidelity-given-attempted is quality ÷ attempted, computable from the two.
- **Why:** The attempted-vs-faithful probe on anchor-examples showed coverage is stable/near-ceiling (31-33 of 33 targets attempted across runs) while concept-fidelity is the volatile layer (17-22 faithful). Attempted is not recall restated: recall was 1.000 on runs where attempted was 31/33 — merge collisions and different-claim writes give perfect recall with missed targets.
- **Validation:** Backfilled `anchor-examples-run2` via `eval rescore` → `anchor-examples-run2-attempted`: opinion_quality reproduced bit-identically (0.5083, proxy cache hit) and opinion_attempted 0.975 matches the probe's 32/33. `eval rescore` can backfill the metric onto any historical experiment; the inspector now reports it.

### critic

- **Hypothesis:** An isolated-context critic tool (claude-opus-4-8 via the Braintrust proxy) reviewing each draft opinion against its cited evidence — rubric: central claim, mechanism, named specifics, whole claim, standalone — should catch the per-proposal concept-drop lottery before proposals are sent, stabilizing the fidelity layer the attempted-vs-faithful probe isolated (attempted ~0.97, faithful 0.51–0.68).
- **Changed:** New `critique_opinion_draft` ToolSpec in `agent.py`: the agent passes its exact draft text + evidence_ids, the host looks up the evidence texts from selected-highlights.jsonl and calls opus-4-8; the rubric lives only in the host-side `CRITIC_PROMPT` (`prompts.py`), plus a "critique each draft before sending, use the final wording verbatim" tool-use bullet. Branched from `exp/anchor-examples` (which already includes the lean-reads efficiency changes), `opinion_attempted` scorer cherry-picked on top. Commit `4209ea3`.
- **Scores:** opinion_quality **0.452** (vs 0.615 score to beat; below the base's worst replicate 0.508). opinion_attempted 0.969, evidence_recall 0.979, evidence_precision 0.522 (+0.015 vs base). Cost of the loop: 137.6k prompt tok/wk (vs 92.6k lean-reads), est $1.63/wk, tool_calls 11.7/wk (~2x), duration 110s/wk.
- **Verdict:** Not promoted; stopped at screen per protocol.
- **Learned:** (a) The critic was heavily used and did its intended job on the flaky class: 88 calls across 9 weeks, 77% REVISE rate, and 7 targets up — mostly the historically flaky concept-drop set (W05-01, W08-04, W10-02/03, W12-01, W13-03, W13-05). (b) But it broke six previously 3/3-stable targets (W05-03, W06-02/03/04, W08-02, W11-01) through three trace-visible mechanisms: **evidence-context asymmetry** — the critic sees only the cited snippet, so it flagged legitimately source-read content as "fabricated... remove it" (W06 ZIRP and vault drafts), directly fighting the bounded-read workflow; **snippet-literalism displacing canon concepts** — demands for the snippet's exact vocabulary and every snippet detail pushed out concepts the draft already carried (W06-03's draft contained "scoping"; the critique-driven rewrite chasing "Sierra"/"onsite" deleted it and the judge failed the final text on exactly that concept); **rewrite churn** — at a 77% REVISE rate nearly every stable draft got re-rolled. (c) The redesign this implies: align the critic with the scoring asymmetry — omission-only (flag missing mechanism/specifics/claim-parts; never demand removals or rewording, since extra content never hurts and the agent reads sources the critic cannot see), no vocabulary pedantry, default READY when unsure, and tell the agent to add flagged concepts by tightening wording rather than deleting others. (d) Moving the fidelity rules out of the main prompt and into the critic (the proposed split) is higher-risk until (c) is proven: in its current form, critique-dominated rewriting degrades canon alignment.
