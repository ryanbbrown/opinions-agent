# Eval Optimization Experiments

Append-only ledger of prompt-optimization experiments, owned by the driver in the **main checkout** — worktree copies are disposable and invisible to each other, so results must be recorded here. One entry per experiment, newest at the bottom. See `eval/GOAL.md` for the protocol. Braintrust (project `opinions-agent`) holds the full scores and traces; this file is the narrative history so the loop can build on prior results without a round-trip.

Cost note: Braintrust's `estimated_cost` is accurate for runs made with thinharness ≥ 0.5.1 (bumped 2026-07-05), which reports OpenAI's `cached_tokens` detail. Experiments recorded before that read ~1.8x high — every input token priced at full rate — so a full 9-week run shown as ~$10 actually cost ~$5.

**Current best:** `exp/critic-2` (anchor-examples lineage + omission-only critic tool; promoted 2026-07-05 on the critic-2-r4 addendum evidence. The 2026-07-06 lean-critic-2 leanness promotion was reversed by Ryan the same day — a 4% prompt reduction is not worth trading the stronger point estimate and precision margin. The critic itself was retained by the 2026-07-12 no-critic ablation: it is a floor lever, not a mean lever.)

**Score to beat:** 0.865 mean `opinion_quality` under scoring_version `2026-07-10-coverage-concepts` (pooled rescores `critic-2-r{1..4}-rs-0710f`: 0.887 / 0.846 / 0.806 / 0.919; floor 0.806), holding replicate-mean `evidence_precision` ≥ 0.50. Fresh runs are graded under the current scoring version, so they compare against this, not the old-scoring numbers. Screen runs compare against the per-model subset baselines pinned in `eval/STATUS.md`. On promotion, update this to the new best's replicate mean.

## Entry format

Each entry records:

- **Experiment** — the variant name. In Braintrust each run is `<variant>-r<N>` (rescores add `-rs-<date>`), with `variant`, `run`, and `scoring_version` metadata for filtering and grouping.
- **Hypothesis** — the change and why it should help.
- **Changed** — one-line summary of the `prompts.py` / `RULES.md` edit.
- **Scores** — mean `opinion_quality` (primary), `evidence_recall`, `evidence_precision`.
- **Verdict** — promoted or reverted, and why.
- **Learned** — anything that should steer the next experiment.

---

### baseline

- **Hypothesis:** Establish the unmodified-prompt scores that every variant must beat.
- **Changed:** none (current `prompts.py` and `RULES.md`).
- **Scores:** opinion_quality 0.358, evidence_recall 0.877, evidence_precision 0.575 (fresh 9-week run under the current binary judge). A prior run's outputs re-judged by the same judge (`binary-judge-rescore`) scored 0.390 / 0.939 / 0.589 — a ~3–6pt run-to-run spread on the same prompt, which is the noise floor variants must clear.
- **Verdict:** current best by default.
- **Learned:** Agent runs are stochastic (proposal count and which evidence converts vary per run); a single run's aggregate is noisy to ±3–6pt.

### fidelity-check

- **Hypothesis:** The dominant `opinion_quality` failure is the agent preserving a stance but dropping a load-bearing piece of the canonical claim. Operationalizing fidelity with a concrete pre-send check should lift quality where concepts were dropped.
- **Changed:** `RULES.md` "Opinion Writing Fidelity" — added a pre-send check that names the four categories most often lost (co-equal second half of a two-part claim; bounding conditions/caveats; named example/term/mechanism/number; the evidence's actual center of gravity vs a substituted thesis). `prompts.py` unchanged.
- **Scores:** opinion_quality 0.308 (−0.050), evidence_recall 0.849 (−0.028), evidence_precision 0.557 (−0.018). Single run.
- **Verdict:** Not promoted. Net-negative on a single run.
- **Learned:** The change has real directional signal — all 4 fail→pass flips (W04-01, W08-01, W08-03, W11-04) are exactly the concept-drop failures it targets. But 6 previously-passing targets flipped to fail (W04-03, W04-04, W05-02, W05-03, W11-03, W13-02), for net −2. With 10 targets flipping in one run and noise at ±3–6pt, a single run cannot separate the 4 real gains from noise churn — this is the concrete case for the multi-run + replicate rule. If revisited: run 2–3x to see whether the 4 gains replicate and the 6 losses are stable or noise, and test a leaner phrasing (the check added ~8 lines) to check whether a tighter version keeps the gains without the churn.

### argument-shape

- **Hypothesis:** The leaner fidelity rewrite suggested by fidelity-check's learned note: replace (not append to) the abstract fidelity section with a "compressed argument" shape — keep the mechanism, the named anchors, the bounds, and both halves of a compound claim — and remove the abstraction pull from "prefer stances over summaries", so concept-drop failures convert without checklist churn.
- **Changed:** RULES.md "Opinion Writing Fidelity" rewritten as the four-part argument shape (mechanism / concrete anchors / bounds / whole compound, with a non-test example); stance bullet gains "taking a side, not abstracting"; prompts.py's duplicate post-read fidelity paragraph consolidated into a pointer at the rules.
- **Scores:** opinion_quality 0.365 (−0.009 vs pooled baseline 0.374), evidence_recall 0.913 (+0.005), evidence_precision 0.573 (−0.009). Single screening run; below the score to beat, so stopped per protocol.
- **Verdict:** Not promoted. Flat — inside the noise band.
- **Learned:** From per-target diffs plus reading the run's actual proposals in the worktree `.runs` DBs: (a) anchor-keeping guidance does move the named-anchor failure class — two stable-fail targets flipped to pass (W08-04, W13-04) and held gains on W04-02/W05-03; (b) unmatched targets rose 3→5 — the W13 trace shows two same-theme arguments from different documents merged into one proposal, so one target consumed the match and the other auto-zeroed; the "whole compound" rule needs an explicit one-argument scope plus an anti-merge granularity rule; (c) the agent systematically skips reflective/personal-register targets (career growth, sustainable ambition, work-satisfaction classes) across weeks — the negative eligibility filters ("obvious practices", "generic career advice") suppress a class the canon actually converts; (d) one causal claim was inverted into a how-to prescription and failed for losing the claim; (e) skips + merge collisions ≈ 0.12–0.15 of mean headroom, the largest quantified lever visible in traces — bigger than remaining wording-fidelity churn.

### coverage-granularity

- **Hypothesis:** Attack the quantified coverage levers from the argument-shape trace review in one coherent redesign: recalibrate eligibility (filter reference material, not topics; personal/reflective stances are first-class; causal claims are stances — don't invert them into recipes), enforce one proposal per argument (anti-merge, with cross-document synthesis only for the same claim), propose revisions when evidence extends an existing opinion, and keep the argument-shape fidelity core (mechanism/anchors/bounds/compound) with an explicit one-argument scope.
- **Changed:** RULES.md — rewrote four Generation Rules bullets ("Avoid empty consensus, not familiar claims", "Filter reference material, not topics", "One proposal per argument", "Extend existing opinions instead of skipping near-duplicates"), strengthened the career/work-life bullet, updated Practical Proposal Test item 2, and replaced the fidelity section with the scoped argument-shape version; prompts.py — narrowed the triage filter ("generic career advice" → "step-by-step tactics without an argued stance") and added revision-spotting to the OPINIONS.md read instruction.
- **Scores:** opinion_quality **0.483 pooled** (runs 0.485 / 0.454 / 0.508, spread 0.054) vs 0.374 baseline (+0.109, outside the ±3–6pt noise band). evidence_recall 0.932 (+0.024). evidence_precision 0.536 (−0.046, above the 0.50 floor).
- **Verdict:** **Promoted.** First run cleared the score to beat, both replicates confirmed; mean beats baseline by ~2× the noise band. Precision cost is real but within the guardrail.
- **Learned:** (a) The coverage levers paid off exactly as quantified: previously-skipped personal-register targets started converting (W06-04 2/3, W08-02 2/3, W10-02 2/3, W11-05 and W12-03 1/3 each), and W04-01/W04-02 improved — 13 targets moved up vs 8 down, with the ups mostly stable-fail→passing and the downs mostly churn among flaky targets; (b) anchor gains from argument-shape held (W08-04 and W13-04 now 3/3); (c) remaining stable fails (0/3) cluster in two groups: stubborn anchor/half-drops (W04-03 specialization-mechanisms list, W04-05 delivery-practice half, W05-04 not-only-income half, W06-01 Figma-position framing, W08-01 floor-not-result, W11-04 "operational scars", W12-01 ownership-as-distinct, W13-01 Shortcut example) and enumerated-list drops (W13-03 named feedback sources); W13-05 still never converts — the revise-existing rule did not produce a revision proposal for it; (d) W11-02 went unmatched 1/3 — watch for residual merging in W11; (e) precision drift (−0.046) is the cost of broader eligibility; further eligibility loosening has little headroom left before the 0.50 floor.

### opus-4-8-medium

- **Hypothesis:** User-requested model experiment (not a prompt variant): run the current-best prompts (`exp/coverage-granularity`) on `anthropic:claude-opus-4-8` with adaptive thinking and effort medium, instead of `openai:gpt-5.5` medium.
- **Changed:** `config.py` model → `anthropic:claude-opus-4-8`; `agent.py` — provider-aware extra_body (`thinking: adaptive` + `output_config.effort` for Anthropic; the OpenAI-style `reasoning` dict would 400), structured output via `output_mode="auto"` (Anthropic resolves to tool mode; OpenAI stays native), explicit `AnthropicMessagesModel` with `max_tokens=16000` (thinharness defaults Anthropic to 1024), request timeout 600s. Prompts/RULES.md unchanged from `exp/coverage-granularity`.
- **Scores:** opinion_quality 0.475 (vs 0.483 pooled current best — a wash on one run), evidence_recall 0.922, evidence_precision **0.627** (+0.091 over current best — the best precision of any run so far). ~2.9 tool calls/week vs ~6.4 (reads sources far less), wall-clock ~12 min vs ~5 min per 9-week run.
- **Verdict:** Not promoted within the prompt loop (single run, below the 0.483 score to beat; a model swap is also outside the prompt-lever edit scope, so adopting it is the user's call). Branch `exp/opus-4-8-medium` kept for reference.
- **Learned:** Model choice changes the failure *profile* more than the mean: Opus flipped several stubborn stable-fails to pass (W06-01 Figma framing, W13-05 revise-existing, W04-01, W05-01, W10-02, W11-05) but dropped targets gpt-5.5 passes reliably (W04-02 one-way ratchet 3/3→0/1, W13-04 3/3→0/1, W05-03, W08-02). Its precision (+0.09) suggests tighter evidence triage with far fewer source reads. If the user wants to pursue Opus, replicates plus effort=high would be the natural next probe; an ensemble insight — some targets seem model-dependent — also hints remaining prompt headroom is real on either model.

### keep-the-list

- **Hypothesis:** The two dominant remaining failure clusters after coverage-granularity are enumerated members compressed away (the "tools, data, skills, and harnesses" class) and corrections-of-a-default dropped (the "goal is NOT maximum delegation" class). Adding two targeted fidelity bullets — keep the full enumeration by name; state the correction, not just the positive replacement — should convert those without new churn.
- **Changed:** RULES.md fidelity section only — added "The full enumeration" bullet and rewrote "The bounds" into "The bounds and corrections" (net +1 bullet). Branched from `exp/coverage-granularity`.
- **Scores:** opinion_quality **0.615 pooled** (runs 0.487 / 0.698 / 0.660, spread 0.210) vs 0.483 current best (+0.133). evidence_recall 0.967 (+0.035; r2 hit 1.000). evidence_precision 0.532 (−0.004, above the 0.50 floor).
- **Verdict:** **Promoted.** Mean beats the score to beat by ~2–4× the historical noise band even counting the low run-1 outlier; both replicates individually landed 0.66+. Consistency is the concern: 0.21 spread is the widest of any variant, driven by run 1 (0.487) — runs 2–3 agree closely.
- **Learned:** (a) The enumeration/correction bullets converted their target classes: W11 became near-perfect (W11-01/02/03/05 3/3, W11-04 "operational scars" 2/3), W12-01 2/3, W13-03 2/3, W13-05 2/3 (first conversions ever for the last three), plus W05-01/W06-04/W08-02 3/3; (b) net +13 targets up / 4 down vs coverage-granularity, and the downs (W04-02, W05-02, W08-04, W10-02) are anchor-class targets that were previously stable — watch whether the longer fidelity list is starting to crowd out the "concrete anchors" bullet; (c) run-to-run spread grew with the rule count — a future leanness pass (consolidating the six fidelity bullets) is a candidate simplification experiment; (d) remaining stable fails are W04-03 (still drops the specialization-mechanisms list) and W04-05/W06-01/W13-01 (named-example class), giving a concrete next hypothesis: the enumeration rule works for claim-member lists but not for lists that live inside a *named example*.

### lean-reads

- **Hypothesis:** User-directed experiment: the current best prompts (`exp/keep-the-list`) plus main's read-efficiency changes — targeted `jsonl_search` by opinion_id on OPINIONS_SOURCES.jsonl instead of a wholesale read, the decision log made write-only (never read), and explicit row schemas for both artifacts in the prompt — should keep quality while cutting wasted reading.
- **Changed:** Cherry-picked main's efficiency commit (`5295255`) onto `exp/keep-the-list`; the one conflict (the read-instructions paragraph in `prompts.py`) was resolved by keeping keep-the-list's revision-spotting clause on the OPINIONS.md read plus main's targeted-sources policy and decisions-read removal. RULES.md unchanged from keep-the-list.
- **Scores:** opinion_quality 0.548 (vs 0.615 score to beat; inside keep-the-list's 0.487–0.698 replicate range), evidence_recall 1.000, evidence_precision 0.508 (just above the 0.50 floor). Single run per user instruction. Efficiency: prompt_tokens/wk 92.6k vs keep-the-list's 97–125k (~13% below the pooled mean), est cost $1.11/wk vs $1.15–1.41; tool_calls/wk essentially unchanged (6.78 vs 6.56–6.89).
- **Verdict:** Not promoted (single run below the score to beat), but no evidence the efficiency changes hurt quality — the run lands mid-range of keep-the-list's own replicates, and the per-target diff is churn-shaped (10 up / 10 down) rather than a systematic regression.
- **Learned:** (a) The token savings are real but modest — the wholesale sources/decisions reads were only ~5–25k prompt tokens/wk; tool-call count didn't drop because the agent still does the same triage reads; (b) this run's downs cluster in one bad week (all four W08 targets 0/1, plus W06-04 and W11-05 which keep-the-list passed 3/3) while W10-02/W10-03/W13-03/W13-05 all flipped up — replicates would be needed before reading anything target-level into the read-policy change; (c) precision at 0.508 is the closest any run has come to the 0.50 floor — watch it if this branch becomes the base for future variants.

### judge-variance probe (diagnostic, not a prompt experiment)

- **Question:** How much of the run-to-run `opinion_quality` spread (keep-the-list: 0.487/0.698/0.660) is judge inconsistency rather than proposal variance?
- **Method:** (a) Re-judged identical stored outputs with the proxy cache disabled (`x-bt-use-cache: never` — note `eval rescore` alone is useless for this: the Braintrust proxy caches temperature-0 judge calls, so a plain rescore returns bit-identical verdicts). keep-the-list run 1 judged 3x, run 2 judged 3x. (b) For the 17 targets whose verdicts flip across keep-the-list runs 1–3, compared the matched proposal texts (difflib similarity) to attribute each flip.
- **Findings:** (a) Judge noise on identical outputs is real but small: run 1 scored 0.4875 all 3 times (0 flips, matches the stored 0.487); run 2 scored 0.635/0.635/0.667 vs 0.698 at run time — a ~3–6pt session-level drift from 1–2 borderline verdicts per judging (W06-03 was one). (b) Of the 17 cross-run flip targets, 16 had materially different matched proposal texts (sim 0.01–0.56) → proposal variance; 1 (W05-03, sim 1.0, pass/pass/unmatched) was a greedy-matching artifact, not a judge flip.
- **Implications:** The 0.21 keep-the-list spread is dominated by proposal variance, not the judge; the protocol's ±3–6pt noise band is exactly the judge+matching floor and stays valid. Borderline proposals are where the judge wavers — decisive proposals judge deterministically. Comparing runs judged on different days carries up to ~6pt of judge-session drift, so same-day replicates are the cleanest comparisons; a variant's gain must clear the band regardless. Fixing the variance means stabilizing the agent's proposals (e.g., the planned fidelity-leanness pass), not the judge.

### anchor-examples

- **Hypothesis:** The four never-passing named-example targets (W04-03, W04-05, W06-01, W13-01) fail because the agent states the abstracted lesson and drops the named case the argument reasons through, or paraphrases the source's own category terms. Strengthening the "concrete anchors" bullet in place (no new bullet, to avoid crowding) — illustrative cases are part of the claim; use the argument's own vocabulary — should convert them.
- **Changed:** RULES.md "The concrete anchors" bullet rewritten in place. Branched from `exp/lean-reads` (deviation from "branch from current best" flagged to the user: the read-efficiency changes are production behavior on main and were quality-neutral; score to beat stays 0.615).
- **Scores:** opinion_quality **0.623 pooled** (runs 0.683 / 0.508 / 0.677, spread 0.175) vs 0.615 — +0.008, inside the noise band. evidence_recall **1.000 pooled** (all three runs 1.000 — first variant with perfect recall). evidence_precision 0.5065 (−0.025 vs keep-the-list; 0.007 above the floor).
- **Verdict:** Not promoted. Screen passed (0.683) but the replicate mean is a wash; precision is drifting toward the 0.50 floor.
- **Learned:** (a) W04-05 converted (0/3→2/3) — the case/vocabulary guidance moves the paraphrase class; (b) reading the matched proposals for the three still-dead targets shows they are not simple name-drops: W04-03 consistently centers the *other half* of the source argument (generalist-vs-specialist) and omits the prescription half (where specialization should live); W06-01 now names the case in 2/3 runs but centers a different framing than the canon; W13-01 compresses the architecture well but never carries the anchoring example, and one run still paraphrased the source's key terms. Two consecutive fidelity-wording experiments have failed to move these three — the residual failure is argument-centering, and more bullet text shows diminishing returns; (c) counterbalancing regressions (W04-01 2/3→0/3, W08-01 1/3→0/3, W08-03, W13-04/05 down) look like proposal-variance churn per the judge-variance diagnostic; (d) consecutive non-promotions since keep-the-list: 2 (lean-reads, anchor-examples) — the next miss triggers the plateau stop.

### lean-fidelity

- **Hypothesis:** The judge-variance diagnostic attributed the run-to-run spread to proposal variance, and the ledger's standing suspicion was fidelity-bullet crowding. Consolidating the five fidelity bullets into three non-overlapping ones (mechanism / named specifics incl. cases, enumerations, vocabulary / whole claim incl. corrections, bounds, both halves) should stabilize proposals while preserving every concept class — promotable by beating 0.615 or by leanness-within-noise.
- **Changed:** RULES.md fidelity section only: five bullets merged into three, ~35% shorter, no concept class removed. Branched from `exp/lean-reads`.
- **Scores:** opinion_quality 0.446 (−0.169 vs 0.615 — far outside the noise band), evidence_recall 0.948, evidence_precision 0.495 (below the 0.50 floor). Single screening run, stopped per protocol.
- **Verdict:** Not promoted — fails both promotion paths (quality collapse, and leanness requires holding scores within noise).
- **Learned:** (a) The consolidation actively hurt: quality fell back toward coverage-granularity territory (0.483), as if the enumeration/correction gains that keep-the-list earned partially evaporated when those rules stopped being separate, explicit bullets. The five-bullet section's redundancy was load-bearing — one bullet per failure class appears to be what the agent actually follows; (b) future leanness passes on RULES.md should trim qualifiers and examples inside bullets, not merge rule categories; (c) this is the third consecutive non-promotion (lean-reads 0.548, anchor-examples 0.623 pooled, lean-fidelity 0.446) — **plateau stop condition met**; the loop ends with `exp/keep-the-list` (+ production efficiency changes = `exp/lean-reads`) as the final best at 0.615.

### opinion_attempted metric (eval infrastructure, not a prompt experiment)

- **What:** A second LLM judge alongside `opinion_quality`, landed on main (`f81a233`). `opinion_attempted` is the lenient layer of the funnel: a target counts as attempted when its matched proposal expresses the same central claim, even if it dropped supporting concepts, named examples, numbers, or caveats. A quality pass implies attempted, so the lenient judge only runs on matched-but-failed targets (~10-15 extra sonnet calls per run). Both judges share one match-and-judge pass per week, so matching is never duplicated and the two metrics can't disagree about which proposal a target matched. The metric triad now reads as a funnel: evidence_recall (citations used) → opinion_attempted (right claims proposed) → opinion_quality (right claims written completely); fidelity-given-attempted is quality ÷ attempted, computable from the two.
- **Why:** The attempted-vs-faithful probe on anchor-examples showed coverage is stable/near-ceiling (31-33 of 33 targets attempted across runs) while concept-fidelity is the volatile layer (17-22 faithful). Attempted is not recall restated: recall was 1.000 on runs where attempted was 31/33 — merge collisions and different-claim writes give perfect recall with missed targets.
- **Validation:** Backfilled `anchor-examples-r2` via `eval rescore` → `anchor-examples-r2-attempted`: opinion_quality reproduced bit-identically (0.5083, proxy cache hit) and opinion_attempted 0.975 matches the probe's 32/33. `eval rescore` can backfill the metric onto any historical experiment; the inspector now reports it.

### critic

- **Hypothesis:** An isolated-context critic tool (claude-opus-4-8 via the Braintrust proxy) reviewing each draft opinion against its cited evidence — rubric: central claim, mechanism, named specifics, whole claim, standalone — should catch the per-proposal concept-drop lottery before proposals are sent, stabilizing the fidelity layer the attempted-vs-faithful probe isolated (attempted ~0.97, faithful 0.51–0.68).
- **Changed:** New `critique_opinion_draft` ToolSpec in `agent.py`: the agent passes its exact draft text + evidence_ids, the host looks up the evidence texts from selected-highlights.jsonl and calls opus-4-8; the rubric lives only in the host-side `CRITIC_PROMPT` (`prompts.py`), plus a "critique each draft before sending, use the final wording verbatim" tool-use bullet. Branched from `exp/anchor-examples` (which already includes the lean-reads efficiency changes), `opinion_attempted` scorer cherry-picked on top. Commit `4209ea3`.
- **Scores:** opinion_quality **0.452** (vs 0.615 score to beat; below the base's worst replicate 0.508). opinion_attempted 0.969, evidence_recall 0.979, evidence_precision 0.522 (+0.015 vs base). Cost of the loop: 137.6k prompt tok/wk (vs 92.6k lean-reads), est $1.63/wk, tool_calls 11.7/wk (~2x), duration 110s/wk.
- **Verdict:** Not promoted; stopped at screen per protocol.
- **Learned:** (a) The critic was heavily used and did its intended job on the flaky class: 88 calls across 9 weeks, 77% REVISE rate, and 7 targets up — mostly the historically flaky concept-drop set (W05-01, W08-04, W10-02/03, W12-01, W13-03, W13-05). (b) But it broke six previously 3/3-stable targets (W05-03, W06-02/03/04, W08-02, W11-01) through three trace-visible mechanisms: **evidence-context asymmetry** — the critic sees only the cited snippet, so it flagged legitimately source-read content as "fabricated... remove it" (W06 ZIRP and vault drafts), directly fighting the bounded-read workflow; **snippet-literalism displacing canon concepts** — demands for the snippet's exact vocabulary and every snippet detail pushed out concepts the draft already carried (W06-03's draft contained "scoping"; the critique-driven rewrite chasing "Sierra"/"onsite" deleted it and the judge failed the final text on exactly that concept); **rewrite churn** — at a 77% REVISE rate nearly every stable draft got re-rolled. (c) The redesign this implies: align the critic with the scoring asymmetry — omission-only (flag missing mechanism/specifics/claim-parts; never demand removals or rewording, since extra content never hurts and the agent reads sources the critic cannot see), no vocabulary pedantry, default READY when unsure, and tell the agent to add flagged concepts by tightening wording rather than deleting others. (d) Moving the fidelity rules out of the main prompt and into the critic (the proposed split) is higher-risk until (c) is proven: in its current form, critique-dominated rewriting degrades canon alignment.

### critic-2

- **Hypothesis:** Realign the critic with the scoring asymmetry: omission-only critique (flag missing mechanism / named specifics / claim-parts; never demand removals or rewording — extra content never hurts the judge, and the agent reads sources the critic cannot see), default READY when unsure, and agent-side instruction to fold flagged concepts in without deleting concepts the draft already carries. Should keep critic-1's gains on the flaky concept-drop class without breaking stable targets.
- **Changed:** `CRITIC_PROMPT` rewritten (omission-only, three checks, no central-claim recentering, no vocabulary demands) and the critique tool-use bullet adjusted. One prompt-only commit (`e1d98e9`) on `exp/critic-2`, branched from `exp/critic`.
- **Scores:** opinion_quality **0.653 pooled** (runs 0.790 / 0.598 / 0.571, spread 0.219) vs 0.615 score to beat (+0.038) and vs its own base anchor-examples 0.623 pooled (+0.030). opinion_attempted 0.954 pooled (0.944/0.944/0.975). evidence_recall 0.986. evidence_precision 0.515 pooled (all runs above the 0.50 floor). Cost: ~118k prompt tok/wk, $1.39/wk, 10.4 tool calls/wk, zero tool errors — cheaper than critic-1 (fewer REVISE cycles).
- **Verdict:** Not promoted under the pinned rule — +3.8pt is inside the ±3–6pt noise band. Logged as borderline-positive: the screen run (0.790) is the highest single run ever recorded, and the design is validated as non-harmful, but the replicate mean does not clear the band.
- **Learned:** (a) The omission-only redesign removed critic-1's harm: same-lineage, near-same-day comparison critic 0.452 → critic-2 0.653 pooled — the entire 0.34 screen-to-screen swing came from what the critic is allowed to say. (b) Per-target vs base: 10 up / 6 down, and the ups include two first-ever conversions on historically dead targets — W04-01 (0/3 → 2/3) and W06-01 (0/3 → 1/3, first pass ever for the Figma argument-centering target) — plus W05-01, W12-01, W13-03, W13-04 now 3/3. The downs (W05-03 matching artifact, W08-02, W08-04, W13-05) look like proposal-variance churn. (c) The critic stabilized coverage (attempted spread 0.031) but not quality spread (0.219) — the concept lottery moved, it did not close. (d) Candidate follow-ups: the fidelity-split experiment on top of critic-2 (shorten the main prompt, critic owns the quality definition), and a cheaper critic model if the tool is kept. Extra replicates to resolve the borderline are possible but are optional-stopping; treat any such decision as a judgment call, not protocol.

### opinion_brevity metric (eval infrastructure, not a prompt experiment)

- **What:** A deterministic fifth scorer alongside the judges: per week, mean proposal word count vs mean golden-target word count, scored `min(1, target_mean / proposal_mean)` — 1.0 at or below the golden length, 0.5 at twice it. Raw means live in the score metadata. Reference metric, not a gate: quality already guards under-writing, so shorter than golden caps at 1.0. Not backfilled; runs and rescores from now on carry it. No `scoring_version` bump — adding a metric changes no existing score, so cross-experiment comparability holds.
- **Why:** The golden targets average 33.6 words, baseline matched them (33.0), and each fidelity layer added length: keep-the-list ~42, anchor-examples ~42–45, critic-2 ~46–48 (max 94). The optimization loop inflated opinions ~40% past the golden register; length was invisible to the metrics, so nothing pushed back.

### compact-opinions

- **Hypothesis:** The fidelity machinery was tuned to the old maximally-specific targets: golden targets average 33.6 words, baseline matched them (33.0), and each fidelity layer added length (keep-the-list ~42, anchor-examples ~42–45, critic-2 ~46–48). Under the plain-language targets, flipping the "fuller opinion" default to compact-but-complete, scoping the enumeration/anchor/correction bullets to load-bearing members, and scoping the post-read detail rule to details the claim depends on should hold quality while pulling length back to the golden register.
- **Changed:** RULES.md fidelity section — anchors bullet loses the illustrative-case expansion (keeps name/vocabulary preservation, adds "a name the argument merely passes through is decoration"), enumeration bullet scoped to lists that constitute the claim, bounds-and-corrections trimmed to one sentence, closing default flipped to "once ingredients are present, shorter is better; length is a cost, not a hedge"; prompts.py post-read paragraph now preserves details "the claim depends on" instead of anything "concrete, faithful, or memorable". Branched from `exp/anchor-examples` (commit `5f60ad8`); main's uncommitted measurement stack (plain-language targets, brevity scorer, variant CLI, thinharness 0.5.1) synced into the worktree uncommitted.
- **Scores:** opinion_quality **0.531** (vs 0.676 score to beat, −0.145; below keep-the-list's worst rescore 0.548). opinion_attempted 0.944. evidence_recall 0.948. evidence_precision **0.497 — marginally below the 0.50 floor**. opinion_brevity 0.86 (first run with the metric): proposals mean 39.4 words / median 38 / max 53, vs anchor-examples ~42–45 and critic-2's max 94. Single screening run, stopped per protocol.
- **Verdict:** Not promoted; failed the screen on quality and the precision floor. The length lever worked (~44 → 39 words, tails gone) but cost quality.
- **Learned:** (a) The compensations are not obsolete under plain-language targets: the retargeting plained the *wording* but kept most load-bearing specifics, and the 12 pass-rate downs cluster exactly in the trimmed bullets' failure classes — enumerations/named specifics (W05-04 four functions, W10-02 Price's Law, W13-03 named feedback sources, W08-04 named failure modes, W12-01 "ownership") and corrections/second halves (W04-05 delivery half, W08-01 "default hook is almost always wrong", W05-02 compounding-without-reliability-dependence). The judge still demands those concepts; only their phrasing got friendlier. (b) Four edits were confounded in one variant; the failure signature points at the bullet trims, not the compact closing — a follow-up keeping the "shorter is better" closing and the scoped post-read rule while restoring the full-strength enumeration/anchors/corrections bullets could separate the length win from the concept drops. (c) Some churn ups are notable (W04-03 passed under its plainer target; W04-02, W11-04, W13-02/W13-04 up vs pooled keep-the-list) but single-run. (d) opinion_brevity behaves as designed and 0.86 is the reference point; the golden register (1.0) was not reached even with an explicitly compact default.

### compact-default

- **Hypothesis:** Unbundle compact-opinions' confounded edits: keep only its two length levers — the "shorter is better" closing default and the post-read rule scoped to details the claim depends on — while restoring the five fidelity bullets to full anchor-examples strength. If compact-opinions' quality drop came from the bullet trims, this should hold quality near 0.676 while keeping the brevity gain.
- **Changed:** RULES.md — only the fidelity closing (fuller-default → compact-but-complete); prompts.py — only the post-read detail paragraph (preserve details "the claim depends on"). Bullets untouched. Branched from `exp/anchor-examples` (commit `a12ef9b`); main's uncommitted measurement stack synced into the worktree uncommitted, as with compact-opinions.
- **Scores:** opinion_quality **0.488** (vs 0.676 score to beat, −0.189; below compact-opinions' 0.531). opinion_attempted **1.000** and evidence_recall **1.000** — the first run with perfect coverage on both. evidence_precision 0.498 (marginally below the 0.50 floor). opinion_brevity 0.835 (proposals mean 40.7 words / max 53). Single screening run, stopped per protocol.
- **Verdict:** Not promoted; failed the screen. The unbundling hypothesis is refuted: restoring full bullets did not recover quality.
- **Learned:** (a) The length pressure itself, not the bullet trims, is what costs quality: with every concept class explicitly protected by a full-strength bullet, the compact closing still produced pure fidelity-layer loss — perfect coverage (attempted 1.000), all evidence cited (recall 1.000), and 17 targets failing on dropped named specifics and second halves. When "shorter is better" collides with "keep every ingredient", the agent shortens by dropping ingredients. (b) The golden targets pack all their concepts into ~34 words; the agent at ~40 words already drops concepts the canon keeps — the gap to golden register is compression skill, not padding, and prompt pressure cannot buy it. Both compact variants paid ~0.15–0.19 quality for ~2–4 words of brevity (0.835–0.86 vs anchor-examples' fuller register). (c) Under the current strict all-concepts judge, brevity and quality trade directly at the margin; pushing brevity through instructions is a dead end — routes that could break the tradeoff are selection-style approaches (generate multiple phrasings, keep the compact-complete one) or accepting the fuller register. (d) Two consecutive screen fails (compact-opinions, compact-default); one more triggers the plateau stop.

### slot-structure

- **Hypothesis:** An anatomy pass over all 33 golden opinions found a consistent generative structure — 2–3 load-bearing clauses: stance (22/33 correcting a default), engine (mechanism 15/33 or named-member enumeration 16/33), payoff (prescription/consequence/rule 19/33 or bound 9/33); named-entity anchors are rare (4/33). Replacing the five "don't drop X" checklist bullets with a generative stance/engine/payoff slot grammar (fill every slot the argument provides; check draft for slots present in the evidence but missing; no length pressure either way) should convert slot-drop failures without the compact variants' quality cost.
- **Changed:** RULES.md fidelity section only, rewritten as the three-slot grammar with each old failure class named inside its slot (corrections in stance; enumerations, both halves, named terms in engine; bounds in payoff). prompts.py untouched. Branched from `exp/anchor-examples` (commit `0430853`); measurement stack synced as with the compact variants.
- **Scores:** opinion_quality **0.600** (vs 0.676 score to beat, −0.076; above compact-opinions 0.531 and compact-default 0.488, inside anchor-examples' 0.565–0.790 rescore range). opinion_attempted 0.975. evidence_recall 0.979. evidence_precision **0.497 — below the 0.50 floor**, third run today at ~0.497. opinion_brevity 0.826 (mean 41.0 words, max 56). Single screening run, stopped per protocol.
- **Verdict:** Not promoted; failed the screen and the precision floor. **Third consecutive non-promotion (compact-opinions, compact-default, slot-structure) — plateau stop condition met again.**
- **Learned:** (a) The grammar's target case converted: W05-02 (the payoff-drop specimen) passed, plus W08-04, W13-02, W13-03, W13-04, W04-03/04 up vs pooled keep-the-list — but the per-target diff is churn-shaped (10 up / 10 down), not a step change. (b) The downs are the same named-specific lottery every variant has shown: Price's Law (W10-02), operational scars (W11-04), the hook (W08-01), Figma (W06-01), the four-functions statement (W05-04), ownership (W12-01). Three structurally different instruction designs today (trimmed checklist, compact default, generative grammar) all leave a per-draft omission rate on named specifics — instruction wording appears exhausted as a lever; the variance is in single-draft generation itself. (c) Brevity landed 0.826 (~41 words) with no length instruction — the grammar bounds length mildly on its own. (d) The anchor-examples lineage rides the precision floor (0.497–0.507 across its runs); any future variant from this base should watch the guardrail. (e) The remaining unexplored lever with mechanism-level support is search: best-of-N draft generation with selection against a slot/ingredient check, since attempted is ~0.95–1.00 everywhere and the failures are per-draft omission noise.

### next direction: best-of-N draft selection (handoff note, not an experiment)

- **What:** The agreed path after the 2026-07-05 plateau: draft N candidate phrasings per opinion and select the most slot-complete one against the evidence, instead of instructing a single draft harder. Full design notes, rubric, base-branch guidance, and session footguns in `.plans/best-of-n-selection.md`; the slot grammar it selects against is documented in `eval/opinion_anatomy.md`.
- **Why:** `opinion_attempted` is 0.94–1.00 on every variant while quality is 0.49–0.68 — the agent proposes the right claims and randomly omits load-bearing elements per draft. Three instruction designs in one day (0.531 / 0.488 / 0.600) left the same omission lottery; the judge-variance diagnostic already attributed spread to proposal variance. Selection attacks the variance directly and, unlike critic-driven rewriting, cannot make a draft worse than its best candidate. A smaller composable probe is also worth screening first or alongside it: slot-structure RULES.md on top of `exp/critic-2`, so the slot grammar shapes the draft and the omission-only critic audits it.

### critic-2-r4 replicate (addendum to critic-2)

- **What:** User-requested fourth generation sample (fresh native run under `2026-07-05-plain-language`, named `critic-2-r4`) to test whether critic-2's floor is really 0.70+ or its rescore pool was tail-lucky.
- **Scores:** opinion_quality **0.831** — the second-highest run ever recorded, from a fresh sample, answering the luck question. Full critic-2 pool under current scoring: {0.877, 0.710, 0.700, 0.831}, mean **0.780**, all four ≥ 0.70, vs keep-the-list {0.548, 0.727, 0.754}, mean 0.676. opinion_attempted 0.975, evidence_recall 1.000, evidence_precision 0.540 (pooled 0.521, above the floor). opinion_brevity 0.695 — the longest opinions of any variant (r4 mean 49.5 words, max 90): the critic buys quality by folding concepts in.
- **Read:** Pooled +0.103 over the score to beat is ~2–3× the noise band with a 0.70 floor across four samples, and the per-target diff vs pooled keep-the-list is 19 up / 6 down — a broad shift, not tail-driven pooling. A rank test at n=4 vs 3 still cannot reach significance (small samples), but by the ledger's promotion rule (replicate mean beats the score to beat by more than the noise band, precision above floor) critic-2 now qualifies. The earlier "0.877 was a lucky run" concern is resolved: r4 reproduced the high end from a fresh generation.
- **Tradeoff:** promotion accepts the longest opinions (brevity 0.695 vs slot-structure's 0.826) for the highest quality; the best-of-N direction (`.plans/best-of-n-selection.md`) is the candidate lever for recovering length without paying quality.
- **Verdict:** **Promoted** (user-confirmed 2026-07-05). Current best is `exp/critic-2`; score to beat 0.780.

### critic-2 counterfactual probe (diagnostic, not a prompt experiment)

- **Question:** Why does critic-2 score 0.78 pooled when its generation instructions (RULES.md + rest of prompts.py) are identical to anchor-examples (0.677), and the critic "didn't seem to do much"?
- **Method:** For each of the four critic-2 samples, extracted every `critique_opinion_draft` call from the run traces (agent traces are experiment-parented, so calls map to weeks via evidence ids), matched calls to the final matched-target proposals, re-judged the **pre-critic draft** of every revised proposal with the standard judge, and recomputed the score as if drafts had shipped unrevised.
- **Findings:** REVISE rate 12–18% (10–12 of ~60–65 calls/run). Direct revision lift per run: r1 **+0.000** (0.877→0.877), r2 **+0.088** (0.623→0.710), r3 **+0.025** (0.675→0.700), r4 **+0.042** (0.790→0.831) — mean **+0.04, never negative** (0 hurt cases across 26 revised-and-matched targets; the omission-only design works as built). The saves are exactly the slot-drop classes: W05-02 payoff, W05-04 four-functions statement, W12-01 ownership correction, W04-05 delivery half, W13-04.
- **Mechanism 1 — the critic clips the left tail.** Lift is inversely related to draft quality: zero on the best run (r1), largest on the worst (r2, 0.623→0.710). That is precisely why critic-2's floor is 0.70+ while keep-the-list bottoms at 0.548: the critic doesn't improve good runs, it rescues bad ones. Replicate-mean comparisons understated this; the floor was the real effect.
- **Mechanism 2 — the audience effect.** Pre-critic drafts average 44.4–47.7 words across all four runs — fuller than anchor-examples' *final* opinions (41.6–44.8) under identical writing rules. Even with zero revisions the counterfactual pool {0.877, 0.623, 0.675, 0.790} means 0.741, still above anchor-examples' 0.677. The only prompt difference is the critique tool-use instruction itself: drafting for an omission-auditor produces fuller drafts. Most of critic-2's extra length (~6 of ~8 words over anchor-examples) comes from this drafting shift, not from revisions.
- **Caveats:** counterfactual verdicts were judged in a different session than the finals (~3–6pt judge-session drift on borderline cases); n=4 runs; pool sizes small. The draft-length shift is consistent across all four runs, which is mechanical evidence independent of judge noise.
- **Implications:** (a) "The critic isn't doing much" was wrong in a specific way — its revisions are modest (+0.04) but its presence restructures drafting, and the two together are the +0.10 over keep-the-list; (b) a critic-less variant (e.g. slot-structure) cannot be expected to replicate 0.78 by copying the writing rules, because the edge is the audit step plus the audience effect, not the wording; (c) slot-grammar RULES.md on top of exp/critic-2 is a composable candidate (grammar shapes the draft, critic audits it); (d) for best-of-N, selection plays the same audit role as the critic — the design should preserve the audience effect by telling the agent its drafts will be judged for slot-completeness.

### slot-critic

- **Hypothesis:** Combine the slot-structure RULES.md grammar with the promoted omission-only critic: the slot grammar should shape the first draft around stance / engine / payoff, while the critic preserves critic-2's audit/audience effect. Performance-first screen; brevity recorded only.
- **Changed:** RULES.md fidelity section only — replaced the five critic-2 fidelity bullets with slot-structure's stance / engine / payoff grammar. Branched from `exp/critic-2` and kept critic-2's `prompts.py` critic instruction.
- **Scores:** opinion_quality **0.721** (vs 0.780 score to beat; screen fail). opinion_attempted 0.950. evidence_recall 1.000. evidence_precision 0.520 (same as critic-2's 0.521). opinion_brevity 0.739 (better than critic-2's 0.695). Single run, stopped per protocol.
- **Verdict:** Not promoted; below the current best by 0.059, so no replicates. The brevity gain does not matter under the performance-first sequencing because quality regressed.
- **Learned:** Slot grammar plus critic improved some critic-2 flaky targets (W08-01/02/03/04, W10-03, W11-01/04, W12-03) and kept precision flat, but it broke a block of formerly high-rate targets (W04-01/02/04, W05-04, W10-02, W13-02/04). The grammar bounded length, but did not preserve critic-2's quality floor; the old explicit fidelity bullets remain load-bearing when paired with the critic.

### best-of-n

- **Hypothesis:** Pure prompt best-of-N: before each proposal, draft three candidate phrasings, compare them for load-bearing slots, choose the most complete candidate, then send that candidate through the existing omission-only critic. This should attack per-draft omission variance without changing the harness.
- **Changed:** prompts.py tool-use instruction only — added three-candidate drafting and self-selection before the existing critique call. Completeness is the selector; directness is only the tie-break for candidates carrying the same slots.
- **Scores:** opinion_quality **0.621** (vs 0.780 score to beat; screen fail). opinion_attempted 0.969. evidence_recall 0.979. evidence_precision 0.523. opinion_brevity 0.750. Cost/runtime similar to critic-2 (~11.2 tool calls/week, ~$0.59). Single run, stopped per protocol.
- **Verdict:** Not promoted; substantially below the current best.
- **Learned:** The self-selection prompt bought shorter opinions but lost load-bearing concepts: W05-02, W05-04, W06-04, W13-01, and several 3/4 critic-2 targets regressed to 0/1. This looks like same-context candidate comparison becoming implicit brevity/directness pressure, not reliable quality search. If best-of-N is revisited, selection likely needs a stronger external check than "compare candidates yourself."

### best-of-n-critic

- **Hypothesis:** Strengthen best-of-N without touching the harness by using the existing omission-only critic as the selector: draft three candidates, critique all three in parallel, choose READY over revised or the candidate with the fewest omissions, then fold in flagged concepts. This should approximate host-side selection while staying inside prompts.py.
- **Changed:** prompts.py critique instruction rewritten so each proposal generates three candidate phrasings and calls `critique_opinion_draft` on all three before choosing the candidate to send.
- **Scores:** opinion_quality **0.702** (vs 0.780 score to beat; screen fail). opinion_attempted 0.969. evidence_recall 0.979. evidence_precision 0.510. opinion_brevity 0.782. Cost/runtime increased materially: ~19.1 tool calls/week, 175k tokens/week, ~$0.75, 134s/week. Single run, stopped per protocol.
- **Verdict:** Not promoted; below current best and much more expensive.
- **Learned:** External candidate criticism recovered quality versus self-selection (0.702 vs 0.621) and produced the shortest screened variant, but still underperformed critic-2 by 0.078 while adding substantial tool cost. The regressions are concentrated in concept-rich targets (W04-01/02/04, W05-02/04, W06-01, W08-03, W10-03, W13-01). The pattern supports the sequencing note: prompt-level selection variants are acting like compression pressure. A true host-side selector may still be different, but it requires an explicit harness change outside the current prompt-only edit scope.

### performance-first round stop

- **What:** Three consecutive performance screens after the critic-2 promotion failed to beat the 0.780 current best: slot-critic 0.721, best-of-n 0.621, best-of-n-critic 0.702. None qualified for replication.
- **Read:** All three improved `opinion_brevity` (0.739 / 0.750 / 0.782 vs critic-2's 0.695) while losing quality. That confirms the user-stated sequencing: brevity should be recorded during performance work and used only as a within-noise tie-break; optimizing it through prompt pressure or prompt-level selection still pays quality.
- **Next:** Current best remains `exp/critic-2`. Further performance work needs a new lever, likely a user-approved host-side selector tool or a separate matching/coverage experiment for unmatched update targets such as W13-05; neither should be smuggled into the prompt-only loop.

### critic-preserve

- **Hypothesis:** The common misses after critic-2 look less like architecture failures and more like the critic accepting softened paraphrases of direct highlights. Tighten only the critic prompt so it flags missing setup clauses, proof standards, temporal qualifiers, "only if" conditions, contrast language, and concrete source anchors when the draft generalizes them away.
- **Changed:** `CRITIC_PROMPT` only, branched from `exp/critic-2` (`512f484`). Added two general omission checks: highlight fidelity and source examples. No eval-specific examples or target text were added; leakage scan passed.
- **Screen:** Targeted weeks `W04 W05 W06 W08 W13`, chosen because they contain the recurring misses. Comparable critic-2 subset baseline is opinion_quality **0.718** (runs 0.870 / 0.670 / 0.560 / 0.770), attempted 0.935, recall 0.983, precision 0.568, brevity 0.758.
- **Scores:** Targeted screen `critic-preserve-r1`: opinion_quality **0.620** (vs targeted critic-2 0.718, −0.098). opinion_attempted 0.910. evidence_recall 0.967. evidence_precision 0.551. opinion_brevity 0.707. Single targeted screen, stopped.
- **Verdict:** Not promoted; the stricter critic prompt failed the targeted screen and still missed W13-05 and W05-03 as unmatched. It also regressed several direct-preservation targets.
- **Learned:** Simply telling the critic to preserve highlighted detail is too blunt: it did not fix update routing or unmatched conversion, and it appears to add friction/churn without improving the final proposal. The failure supports the earlier suspicion that W13-05 is under-contextualized for the critic, not just under-specified in the critic rubric.

### critic-opinions-context

- **Hypothesis:** W13-05 is an update-routing miss: the critic sees cited highlight text but not the current opinion it should preserve/update. Give the critic current `OPINIONS.md` as context and add a narrow existing-opinion update check, while keeping critic-2's omission-only behavior otherwise.
- **Changed:** `build_critic_tool` now passes the current opinions document into `CRITIC_PROMPT`; the prompt tells the critic to flag when cited evidence extends or sharpens a current opinion but the draft omits the current opinion's core claim or named anchor. Added a construction test proving current opinions reach the critic prompt. Branch `exp/critic-opinions-context` (`8de4b0a`). Eval runs used the copied scorer stack with Braintrust proxy cache bypass only to avoid a cached malformed judge JSON response; scoring logic unchanged.
- **Targeted screen:** `critic-opinions-context-r1-rs-nocache` on `W04 W05 W06 W08 W13`: opinion_quality **0.780** (vs targeted critic-2 0.718, +0.063). opinion_attempted **1.000**. evidence_recall **1.000**. evidence_precision 0.535. opinion_brevity **0.791**. It converted W13-05, W08-02, W08-04, W05-03, W05-04, and the W04 direct misses except W04-05. Misses remained W04-05, W06-01, W08-03, W13-01, W13-04.
- **Full screen:** Because the targeted screen cleared, ran full 9-week screen as `critic-opinions-context-r2`: opinion_quality **0.746** (vs 0.780 score to beat, −0.034). opinion_attempted 0.975. evidence_recall **1.000**. evidence_precision 0.522. opinion_brevity **0.755**. Single full run, stopped per protocol.
- **Verdict:** Not promoted; the targeted improvement did not survive the full run. Current best remains `exp/critic-2`.
- **Learned:** Current-opinions context is a real lever for update targets and coverage: the targeted screen converted W13-05 and W05-03, and the full run improved recall and brevity without hurting precision. But the effect is not stable enough; the full run missed W13-05 again and regressed direct-preservation targets such as W04-01, W04-04, W04-05, W05-04, W06-01, W11-04, W12-01, and W13-02. The context may encourage shorter broader revisions (brevity +0.061 vs critic-2) that still drop load-bearing details.

### critic-opinions-recheck

- **Hypothesis:** The agent may be half-applying critic feedback because critic-2 says no recheck is needed after folding in `REVISE` concepts. Keep the current-opinions critic context, but require one more `critique_opinion_draft` call after any revised wording before sending the proposal.
- **Changed:** `TOOL_INSTRUCTIONS` only on top of `exp/critic-opinions-context`: if the critic returns `REVISE`, revise the wording and call the critic once more with the same evidence IDs. Branch `exp/critic-opinions-recheck` (`51580ab`). Leakage scan passed.
- **Scores:** Targeted screen `critic-opinions-recheck-r1` on `W04 W05 W06 W08 W13`: opinion_quality **0.620** (vs targeted critic-2 0.718, −0.098; same quality as critic-preserve). opinion_attempted **1.000**. evidence_recall **1.000**. evidence_precision 0.543. opinion_brevity **0.814**. Single targeted screen, stopped.
- **Verdict:** Not promoted; failed the targeted screen.
- **Learned:** Rechecking revised drafts did not recover fidelity; it mainly increased brevity/coverage while quality collapsed. That suggests the agent is not losing points primarily because it fails to apply one round of critic feedback. The remaining issue is earlier: proposal selection/routing and draft formation still decide whether the right claim and original highlight details are present before the critic can help.

### bon-selector-mimic (recovered entry — ran 2026-07-05 ~20:00, before the critic-context round, but was never recorded)

- **Hypothesis:** The best-of-n-critic post-mortem said prompt-level selection acts like compression pressure and a true host-side selector might not. Add a host-side `select_opinion_draft` tool (opus-4-8 via the Braintrust proxy, temp 0): the agent drafts three candidate phrasings, the selector — prompted with the critic's own omission checklist (mechanism / named specifics / whole claim) — picks the candidate with the fewest omissions and returns FINAL wording, which then still goes through the omission-only critic.
- **Changed:** `agent.py` — new `build_selector_tool` + `_evidence_blocks` helper, selector registered alongside the critic; `prompts.py` — `SELECTOR_PROMPT` (omission-checklist selection, never penalize extra content) and a tool-use bullet (draft three candidates, call selector, use FINAL verbatim, then critique). Construction test added in `tests/test_critic.py`. Branch `exp/bon-selector-mimic`.
- **Scores:** opinion_quality **0.719** (vs 0.780 score to beat; screen fail). opinion_attempted 0.975. evidence_recall **1.000**. evidence_precision 0.527. opinion_brevity 0.734. Single full run, stopped per protocol.
- **Verdict:** Not promoted; below the current best.
- **Learned:** Host-side selection with the critic's omission rubric beats prompt-level self-selection (0.719 vs best-of-n's 0.621) at similar brevity, but only matches best-of-n-critic territory — picking the least-bad of three same-context candidates is not enough.

### bon-selector-auditor (recovered entry — same unrecorded round)

- **Hypothesis:** The mimic selector can only pick among candidates that are all short/polished variants of the same abstraction. Reframe the agent-side instruction: each candidate must be an independently complete serious attempt to carry every load-bearing part of the argument; the selector is an omission auditor in a separate context, not a beauty contest.
- **Changed:** `prompts.py` tool-use bullet only, on top of the mimic harness (branch `exp/bon-selector-auditor`).
- **Scores:** opinion_quality **0.673** (vs 0.780 score to beat; screen fail, below mimic's 0.719). opinion_attempted 0.975. evidence_recall **1.000**. evidence_precision 0.519. opinion_brevity 0.708. Single full run, stopped per protocol.
- **Verdict:** Not promoted.
- **Learned:** Telling the drafter to make each candidate exhaustively complete did not help and slightly hurt — the leverage is not in how the candidates are framed to the drafter but in what the selector is allowed to do with them (see bon-selector-slots).

### bon-selector-slots (recovered + completed 2026-07-05 late session)

- **Hypothesis:** Same harness as bon-selector-mimic, but the selector scores candidates on the slot grammar (stance / engine / payoff) and is allowed to **consolidate slots across candidates** into one final wording, instead of picking the least-incomplete candidate.
- **Changed:** `SELECTOR_PROMPT` only, on top of the mimic harness (branch `exp/bon-selector-slots`). Agent-side instruction is mimic's simple version, not auditor's.
- **Measurement caveat (why this went unrecorded):** the native `bon-selector-slots-r1` hit an Anthropic credit outage mid-run: the judge failed on exactly W04/W05/W06 with "credit balance is too low", so its stored 0.880 `opinion_quality` averages only the five easier weeks and is **not comparable** to any full-run number. Generation was clean (all selector/critic calls for those weeks succeeded before the outage; only 2+2 tool calls failed run-wide, in W07). `bon-selector-slots-r2` aborted during the same outage and holds no scores — it is not a sample. The prior session rescored r1 after credits refilled (`bon-selector-slots-r1-rs-credit-refill`) and ran r3/r4 natively, then recorded none of it.
- **Scores:** opinion_quality **0.768 pooled** over four valid samples (r1-rs-credit-refill 0.787, r3 0.715, r4 0.771, plus this session's fresh `bon-selector-slots-r2-737a749b` 0.800 — named with a collision suffix because the empty r2 held the name) vs 0.780 score to beat (−0.011). Spread **0.085** vs critic-2's 0.177, floor 0.715 vs 0.700. opinion_attempted 0.970. evidence_recall 0.984. evidence_precision 0.519 (above floor). opinion_brevity 0.675 (slightly longer than critic-2's 0.695). All four samples verified full-coverage, error-free on judging, `scoring_version 2026-07-05-plain-language`.
- **Verdict:** Not promoted — pooled mean is a hair below the score to beat, and promotion requires clearing it by more than the noise band. Logged as interesting: equal-within-noise quality with roughly **half the run-to-run spread**.
- **Learned:** (a) Host-side slot-consolidating selection is the first mechanism to visibly compress proposal variance (the judge-variance probe's target): four samples land within 0.085. It buys stability, not mean — the per-target diff vs critic-2 is a trade: the W08 block and W10/W11 flakies firm up (W08-02 4/4 vs 1/4, W08-04 4/4 vs 2/4), while W04/W05 direct-preservation targets sink (W04-04 1/4 vs 3/4, W04-05 0/4 vs 1/4, W05-04 1/4 vs 3/4). W13-05 stays 0/4 unmatched — selection cannot fix routing. (b) Ops lesson: a partial-judging run can silently report an inflated mean; when a run looks like a record, check per-week score coverage before believing it (the runner leaves quality null for weeks whose judge call raised). (c) If a future variant needs variance control, the slots selector is the proven lever to compose with a mean-raising change.

### critic-editorial

- **Hypothesis:** STATUS direction 1 (strong editorial critic): give the critic enough context to make real editorial judgments — the draft and cited evidence plus current `OPINIONS.md` and the week's full selected evidence — and license beyond omission repair: it may say the draft is wrongly centered, should update a named existing opinion, or should be split. Principles-based prompt (what a good proposal looks like) instead of an omission checklist; keeps the no-removals/no-vocabulary-pedantry asymmetry and READY/REVISE format.
- **Changed:** `agent.py` `build_critic_tool` — critic prompt now includes current opinions and all selected week evidence with cited rows marked; `prompts.py` — `CRITIC_PROMPT` rewritten as editorial reviewer, critique tool-use bullet lets the agent recenter/split/update on feedback (splits get one follow-up critique); construction test updated. Branch `exp/critic-editorial` (`a44580a`) from `exp/critic-2`, main's measurement stack synced in the same commit. Leakage scan passed.
- **Scores:** Targeted screen `critic-editorial-r1` on `W04 W05 W06 W08 W13`: opinion_quality **0.610** (vs targeted critic-2 0.718, −0.108). opinion_attempted 0.910. evidence_recall 0.967. evidence_precision 0.566. opinion_brevity 0.805. Full week coverage verified. Cost $0.62. Single targeted screen, stopped per protocol.
- **Verdict:** Not promoted; failed the targeted screen.
- **Learned:** Same signature as critic-opinions-context, amplified: the editorial context/license converts the routing case (W13-05 passed — only its second pass ever) but degrades direct preservation (W04-01/04/05, W05-03 unmatched, W05-04, W08-01, W13-01/02/04 all failed; attempted dropped to 0.910; brevity rose to 0.805, consistent with shorter, broader editorial rewrites). Three critic-context variants now show the same trade, at different strengths: more context and freedom moves routing/coverage misses but costs more preservation than it gains. The editorial-critic path looks exhausted; the preservation problem lives in draft formation, not critique — STATUS direction 2 (better first-draft generation) is the open lever.

### draft-ingredients

- **Hypothesis:** STATUS direction 2 (better first-draft generation): make the drafter enumerate the argument's load-bearing ingredients (stance/correction, mechanism, named specifics and enumerations, both halves, bounds/payoff) before writing, then check the draft against the list before critiquing — so the first draft preserves highlighted claims and the critic only does local repair.
- **Changed:** `prompts.py` only — the post-read detail-preservation paragraph in the workflow instructions rewritten as the draft-from-ingredient-list procedure (replaces, does not append; RULES.md untouched). Branch `exp/draft-ingredients` (`82acd7d`) from `exp/critic-2`, measurement stack synced. Leakage scan passed.
- **Scores:** Targeted screen `draft-ingredients-r1` on `W04 W05 W06 W08 W13`: opinion_quality **0.650** (vs targeted critic-2 0.718, −0.068). opinion_attempted **1.000** and evidence_recall **1.000** — perfect coverage on the miss-heavy subset. evidence_precision 0.570. opinion_brevity 0.743. Cost $0.69. Single targeted screen, stopped per protocol.
- **Verdict:** Not promoted; failed the targeted screen.
- **Learned:** The ingredient list bought perfect coverage but not fidelity. Every one of the 8 failures is a substitution, not an omission — the judge notes repeatedly read "X is replaced with (vaguer Y)": the draft carries a weakened paraphrase of the load-bearing clause, so neither the drafter's self-check ("every listed ingredient appears in some words") nor the omission-only critic (which counts a concept present "in any words") catches it. This sharpens the failure model: the residual quality gap on stubborn targets is *paraphrase degradation* that passes both checks, and instructions to enumerate ingredients cannot fix a check that accepts weakened versions. Any future attempt at this class needs a check that compares strength/specificity against the source clause, not presence — with critic-preserve's failure as a warning that a blunt "stricter critic" makes things worse.

### lean-critic-2

- **Hypothesis:** STATUS direction 3 (prompt simplification): remove the redundant restatement layer from critic-2's prompts while touching no rule category and none of the fidelity section (three prior failures came from touching it). If scores hold within noise, promotable on leanness alone; also tests whether the Practical Proposal Test checklist was load-bearing.
- **Changed:** RULES.md — deleted the Practical Proposal Test (7 questions restating Generation Rules and Core Definition), trimmed a stray qualifier sentence from the empty-consensus bullet, condensed the unresolved-tensions bullet; prompts.py — condensed the post-read paragraph (kept the anti-abstraction clause verbatim). ~120 words total (−8% RULES.md); all 11 Generation Rules categories, the fidelity section, read policy, critic prompt, and Telegram format untouched. Branch `exp/lean-critic-2` (from `exp/critic-2`), measurement stack synced. Leakage scan passed.
- **Scores:** Targeted screen `lean-critic-2-r1` on `W04 W05 W06 W08 W13`: opinion_quality **0.750** (vs targeted critic-2 0.718, +0.032 — the best targeted screen of the 2026-07-05 late session; critic-editorial 0.610, draft-ingredients 0.650). opinion_attempted 0.960. evidence_recall **1.000**. evidence_precision 0.573. opinion_brevity 0.707. W06 and W08 passed every target; fails were the usual set (W04-01/02/04, W05-04, W13-01, W13-05 unmatched). Full week coverage verified.
- **Verdict:** Screen passed — holds (slightly beats) the subset baseline with leaner prompts. **Full-run validation not yet run**: the goal loop was stopped by the user after this screen. To pursue the leanness promotion, run the full 9 weeks 3x from `.worktrees/lean-critic-2` and promote only if the replicate mean holds 0.780 within noise with precision ≥ 0.50.
- **Learned:** The Practical Proposal Test layer was not load-bearing — removing a whole redundant checklist did not hurt the miss-heavy subset at all. Consistent with the lean-fidelity lesson refined: merging rule *categories* hurts, but deleting a pure *restatement layer* is safe. This is the first leanness edit that survived a screen.

### lean-critic-2 — validation addendum (2026-07-06)

- **Runs:** Full 9-week runs `lean-critic-2-r2` **0.806**, `-r3` **0.721**, `-r4` **0.740** → pooled **0.756**, spread 0.086. opinion_attempted 0.990. evidence_recall 0.993. evidence_precision **0.508** (runs 0.510/0.511/0.504 — above the floor but with no margin). opinion_brevity 0.664. Per-week judge coverage hand-verified on all three runs (week means reproduce the reported aggregates exactly).
- **Verdict:** Validated as a hold: 0.756 vs 0.780 is −0.024, well inside the noise band, with every run inside critic-2's observed range (0.700–0.877). Initially promoted on leanness, then **reversed by Ryan the same day**: a 4% prompt reduction is not worth trading the stronger point estimate and precision margin. Current best remains `exp/critic-2`; the branch stays as the proof that the Practical Proposal Test layer is dead weight, to be folded into any future stronger variant.
- **Learned:** The misses are the same substitution set as critic-2's (W04-01/02/04/05, W05-04, W08-02/03, W13-01/04, W12-01); leanness moved nothing on the failure model. Precision sits at 0.504–0.511 across all three runs — tighter to the floor than critic-2's 0.504–0.540 — so any future variant stacked on this branch should watch precision first.

### lean-overhaul-1

- **Hypothesis:** User-directed: a much bigger prompt overhaul — reduce redundancy toward a minimal instruction set — instead of incremental trims. Keep the three known load-bearing pieces verbatim (Opinion Writing Fidelity section, the critique tool-use bullet that creates the audience effect, the post-read preservation paragraph) plus the critic prompt and Telegram message shape; consolidate everything else.
- **Changed:** Everything else: Role 3 paragraphs → 1; the six-bullet read-triage list → one sentence; Telegram reply/approval semantics halved; six file-tool bullets → two; RULES.md Core Definition condensed, the three domain-eligibility bullets merged into one, Confidence → one sentence, Coding-Agent Boundary 4 examples → 1 pair. Built system prompt 2610 → **2043 words** (−22% vs lean-critic-2, −25% vs critic-2). Branch `exp/lean-overhaul-1` (`75d62a9`) from `exp/lean-critic-2`. Leakage scan passed.
- **Scores:** New screen type — strong-week viability screen `W06 W10 W11 W12` (critic-2 subset baseline **0.881**; rationale: an overhaul's first risk is breaking what works, and the strong weeks detect that at ~45% of full-run cost): `lean-overhaul-1-r1` **0.833**, recall 1.000, precision 0.548 — only misses were known coin-flip targets (W10-02, W12-03), no new breakage → graduated. Full 9-week runs: `-r2` **0.775**, `-r3` **0.677**, `-r4` **0.652** → pooled **0.701** vs 0.780 (−0.079, outside the noise band). attempted 0.944, recall 0.976, precision 0.529, brevity 0.641. Coverage verified on all runs.
- **Verdict:** Not promoted; the −25% cut costs a real ~8 points on the full set.
- **Learned:** (a) The cost concentrates on the hard weeks (W04 collapsed to 0.2 in r4) while strong weeks stay fine — the redundancy the overhaul removed was functional there. (b) The strong-week screen is only a breakage tripwire: it passed (0.833) a variant that failed the full set (0.701). Leanness *holds* must be validated on the full set (or at least the miss-heavy subset). (c) Brevity got worse, not better (0.641 vs critic-2's 0.695) in every run — fewer instructions produced longer proposals. (d) The failure mode stayed the substitution family; no new mechanism, just more of it.

### lean-overhaul-2

- **Hypothesis:** Overhaul cycle 2: the remaining fat is cross-section duplication — the approval-gating / edit-after-direction / validate-before-done contract restated in Role, Evidence And Workflow, and the artifact rules; Coding-Agent Boundary duplicating the reference-material filter. State each contract exactly once.
- **Changed:** Deduplicated contract statements across sections; merged Coding-Agent Boundary into the filter-reference-material rule; tightened Generation Rules second sentences. 2043 → **1809 words** (−34% vs critic-2). Branch `exp/lean-overhaul-2` (`9c8aecd`) from `exp/lean-overhaul-1`. Leakage scan passed.
- **Scores:** Strong-week screen `lean-overhaul-2-r1`: opinion_quality **0.742** (vs 0.881, far outside noise). opinion_attempted 0.888, evidence_recall **0.888** — two targets never proposed at all (W06-04, and W11-05 which had never been flaky in any prior run). precision 0.528, brevity 0.645. Coverage verified. Single screen, stopped.
- **Verdict:** Not promoted; failed the screen with a *coverage* regression, not a fidelity one.
- **Learned:** Among the cuts, the one that plausibly reduces proposal generation is the workflow sentence "Send Telegram messages for any conceptual opinion changes Ryan should approve, reject, revise, or discuss" — after the dedup, the proposal duty existed only as a clause in the Role line. Tested directly by lean-overhaul-2b.

### lean-overhaul-2b

- **Hypothesis:** If the propose-for-approval duty sentence is the load-bearing piece cycle 2 cut, restoring only that sentence should recover coverage while keeping the −33% prompt.
- **Changed:** Restored that single sentence on top of `exp/lean-overhaul-2` (1809 → 1823 words). Branch `exp/lean-overhaul-2b` (`c921298`). Leakage scan passed.
- **Scores:** Strong-week screen `lean-overhaul-2b-r1`: **0.917**, attempted 1.000, recall **1.000** — coverage fully recovered, only miss the known coin-flip W10-02. Diagnosis confirmed. Full 9-week run `lean-overhaul-2b-r2`: **invalid-partial** — three concurrent full runs triggered OpenAI 429 quota errors that killed the W10 and W12 rows (verified: the six surviving weeks average exactly the reported 0.6167). On the six valid weeks (which include all the hard ones) it scored **0.617 vs critic-2's same-six-week baseline 0.748** (−0.131), the unproposed-target regression returned at full scale (W05-03, W06-04, W11-05 unmatched), and precision was 0.455 — below the floor. Stopped without a rerun; the valid six-week evidence is decisive.
- **Verdict:** Not promoted; the overhaul path is dead. Current best remains `exp/lean-critic-2`.
- **Learned:** (a) Restoring the duty sentence fixes coverage on easy weeks but not under full-set load — the deeper cuts still degrade hard-week fidelity *and* coverage, so cycle 1's deleted redundancy was functional beyond that one sentence. (b) The strong-week screen flattered the overhaul family twice (0.833→0.701, 0.917→0.617); treat it strictly as a cheap breakage tripwire, never as validation. (c) The one durable positive: the proposal-duty sentence is now a known single point of failure — the agent's core output obligation must be stated explicitly in the workflow section, not implied by the Role line. (d) Ops: cap concurrent full runs at 2 — the third simultaneous run drew OpenAI 429 quota kills; and the per-week coverage check caught the partial-judging artifact again (reported 0.617 ≠ verdict-implied 0.712 was the tell).

### drafter-model screen (gpt-5.6 + opus-4.8-high) — 2026-07-12

- **Hypothesis:** User-requested model screen (not a prompt variant): keep the `exp/critic-2` agent fixed and swap only the *drafter* model (`OPINION_AGENT_MODEL`; the critic tool stays opus-4-8) to see whether a cheaper OpenAI model (gpt-5.6-luna, gpt-5.6-terra) holds gpt-5.5's quality, or a stronger one (opus-4.8 at high effort) beats it. All runs generated in worktrees, then rescored from main under the current `2026-07-10-coverage-concepts` coverage judge. Score to beat: gpt-5.5-medium critic-2 pooled **0.865** (4 runs), holding precision ≥ 0.50.
- **Changed:** No prompt/RULES edits. `config.py` drafter model per probe. OpenAI probes (luna/terra) ran unchanged on the `exp/critic-2` worktree (OpenAI-shaped `extra_body={"reasoning":{"effort":…}}`). Opus ran on the pre-existing `exp/critic-2-opus48` worktree, which combines the critic-2 agent with Anthropic plumbing (`extra_body` → `{"thinking":{"type":"adaptive"},"output_config":{"effort":…},"max_tokens":64000}`); its `agent.py` shipped with **no `request_timeout` override**, so it inherited thinharness's 120s default — added `request_timeout=1800` to `build_harness_config` (uncommitted in that worktree). Rescore experiments: `critic-2-{luna-high,terra-medium,opus48-high}-r{…}-rs-0710f`.
- **Scores** (5 metrics under the coverage judge; single run each unless noted):
  - **gpt-5.6-luna high:** opinion_quality **0.681**, attempted 0.944, recall 0.934, precision 0.510, brevity **0.384** (≈2.6× golden length). Coherent but ~18pt below gpt-5.5 and bloated.
  - **gpt-5.6-terra medium:** opinion_quality **0.388**, attempted 0.610, recall 0.333, precision 0.891, brevity 0.728. Collapses on coverage — under-produces proposals (per-week quality W10=0.00, W12=0.00 shut out); the high precision is an artifact of barely producing, not a win. User hypothesis "≈ gpt-5.5-medium but cheaper" is decisively false.
  - **opus-4.8 high — r1 partial-FAILED:** 4 of 9 weeks (W05, W08, W11, W13, incl. the miss-heavy ones) died on `httpx.ReadTimeout` at the 120s default; the 5 survivors were the easy weeks, so its headline numbers were incomplete *and* upward-biased. Not reportable. **r2 (timeout fixed, clean 9-week):** opinion_quality **0.819**, attempted 0.875, recall 0.867, precision **0.533** (best of the group), brevity **0.356** (worst — ≈2.8× golden length). Per-week quality: **W04=0.00**, W05=1.00, W06=1.00, W08=0.75, W10=1.00, W11=0.80, W12=1.00, W13=1.00 → excl. W04 the other 7 weeks average **0.936**. $1.94/run, ~11 min wall-clock. W04 was a clean routing refusal: `proposals: []` for the whole week (zero proposals emitted), not a concept miss.
- **Verdict:** None promoted; gpt-5.5-medium (0.865) remains the drafter. luna and terra both regress and are rejected. opus-4.8-high is the only model with a ceiling *above* gpt-5.5 (0.936 on the 7 weeks it engaged, best precision) but its raw macro (0.819) sits below gpt-5.5 entirely because of the W04 shutout, and it is verbose and under-attempts — so it is not a drop-in win; realizing it needs prompt/threshold work (stop the W04-style "nothing here is worth proposing" refusal, rein in length), not a raw swap.
- **Learned:** (a) The metric that matters (coverage quality) is exactly where cheaper OpenAI models fail — luna is coherent-but-worse-and-verbose, terra can't clear the coverage bar at all; cost savings don't survive the judge. (b) Opus changes the failure *profile*, not just the mean (same finding as `opus-4-8-medium`): it peaks on hard weeks but drops whole weeks it deems unworthy — here all of W04, consistent with the medium probe dropping W04-02's "one-way ratchet". The reliability/verbosity gap, not the ceiling, is what keeps it behind gpt-5.5. (c) Infra: any Anthropic drafter needs `request_timeout` raised well above thinharness's 120s default or high-effort weeks silently time out and bias the survivors toward the easy weeks — the same null-week/shrunk-denominator tell the STATUS ops-note warns about, now reproduced by a timeout rather than a 429. (d) `completion_reasoning_tokens` reads 0 in Braintrust for *all* of these models including gpt-5.5, so it is a reporting artifact, not evidence that reasoning effort failed to engage.

### no-critic ablation (critic-2 prompt, critic step removed) — 2026-07-12

- **Hypothesis:** Now that the coverage judge is less brittle, test whether the critic is still earning its place. The suspicion is that the critic used to rescue bad scores that were partly artifacts of an over-specific judge; under the current judge, the same system prompt *without* the critic step might match critic-2. If so, the critic is dead weight and the agent simplifies to a single drafting pass.
- **Changed:** No rubric change. Branch `exp/critic-2-no-critic` off `exp/critic-2`: removed `build_critic_tool(...)` from the `_run_harness` tools list (`agent.py`) and removed the "call `critique_opinion_draft` before sending each proposal" bullet from `TOOL_INSTRUCTIONS` (`prompts.py`); `build_critic_tool`/`CRITIC_PROMPT` left as dead code. Drafter unchanged (gpt-5.5, medium). Clean pair r3/r4 (r1/r2 were voided by a `.runs/` dir collision when two processes shared a run dir; re-ran with distinct `RUNS_DIR` per process). Rescored from main under `2026-07-10-coverage-concepts`.
- **Scores** (no-critic pooled over r3/r4, vs critic-2 pooled over r1–r4):
  - **opinion_quality 0.806** (r3 0.846, r4 0.765) vs critic-2 **0.865** (range 0.806–0.919). Not a match: mean −0.06, and the floor fell to **0.765 — below any critic-2 run**.
  - **opinion_attempted 0.960** (r3 1.000, r4 **0.919**) vs critic-2 0.960 (tight 0.944–0.975). Equal mean but r4 dropped below critic-2's floor — the critic also stabilizes whether borderline proposals get emitted.
  - **evidence_recall 0.985**, **evidence_precision 0.512** (r3 0.503, r4 0.520) — both ≈ critic-2 (0.990 / 0.521); precision still clears the 0.50 floor.
  - **opinion_brevity 0.777** (r3 0.794, r4 0.759) vs critic-2 0.706 — removing the critic makes opinions **shorter**.
  - Per-week tell: **W12 held 1.00 in all four critic-2 runs but fell to 0.67 in both no-critic runs** — a reproducible concept the critic catches. no-critic also regressed W05 (0.50/0.75 vs critic-2's 0.75–1.00) while gaining on W08/W13 — the same "redistribute failures" signature seen elsewhere.
- **Verdict:** Not promoted; critic retained. It is no longer a *mean-lift* lever, but it is a *floor* lever: removing it drops quality below anything critic-2 produces and loosens attempted, at the cost of some length. The user's "the critic was only rescuing judge artifacts" hypothesis is partly right (mean gap is small) but incomplete — a real reproducible rescue (W12) survives the better judge.
- **Learned:** (a) Under the current judge the critic buys **downside protection, not average quality** — mean −0.06 but floor −0.041 and a below-range attempted run; its job shifted from "rescue bad scores" to "cap the bad tail". (b) It trades brevity for that floor: the "add the missing concept" pressure lengthens drafts (+0.071 brevity when removed). (c) Ties to the same-session read-consistency analysis: reads are consistent across runs and the score variance lives entirely in the drafting step, so the critic functions as a **draft-variance backstop** (catch a dropped checklist concept *after* drafting), not a read-compensator — which is exactly why removing it lowers the floor rather than the mean. (d) n=2 caveat: clean pool is only two runs (r1/r2 voided), directionally clear but under-powered; r5/r6 deferred. The open lever is still first-draft concept-naming consistency (STATUS direction 2), which would shrink the tail the critic is currently backstopping.

### screen-subset baselines + structural-explicitness round kickoff (diagnostic, not a prompt experiment) — 2026-07-12

- **What:** Derived per-week `opinion_quality` for every drafter model with rescores stored under `2026-07-10-coverage-concepts`, to pick a cheap screen subset for the multi-model structural-rewrite round. Computed from per-target verdicts via `inspect_experiment.py`; mean-of-week-means reproduces each run's reported aggregate exactly.

  | week (targets) | gpt-5.5 med (critic-2 pooled, n=4) | opus-4.8 high (n=1) | luna high (n=1) | terra med (n=1) |
  | --- | --- | --- | --- | --- |
  | W04 (5) | 0.70 | 0.00 (week refusal) | 0.40 | 0.20 |
  | W05 (4) | 0.875 | 1.00 | 0.75 | 0.75 |
  | W06 (4) | 0.875 | 1.00 | 1.00 | 0.25 |
  | W08 (4) | 0.75 | 0.75 | 0.50 | 0.50 |
  | W10 (3) | 0.917 | 1.00 | 0.33 | 0.00 |
  | W11 (5) | 1.00 | 0.80 | 1.00 | 0.80 |
  | W12 (3) | 1.00 | 1.00 | 0.67 | 0.00 |
  | W13 (5) | 0.80 (W13-05 0/4) | 1.00 | 0.80 | 0.60 |

- **Read:** "Easy vs hard" is model-dependent. W04 is the universal hard week and the refusal week (opus emitted zero proposals; it also collapsed to 0.2 when lean-overhaul gutted prompt structure — it is the structure canary). W10/W12 are near-ceiling for gpt-5.5 but exactly where cheap models collapse, and W12 is the critic's one reproducible save (fell to 0.67 in both no-critic runs). Opus converts W13-05 — the routing target gpt-5.5 has never matched (0/4) — so that failure is prompt-addressable, not impossible.
- **Subset choice: `W04 W10 W12 W13`** (16/33 targets, ~48% full-run cost). Covers all four failure modes the explicitness rewrite targets: refusal/eligibility (W04), direct preservation (W04-01/02/04/05), routing (W13-05), named-specific drops (W10-02/03, W13-01/02/04), plus the critic-floor tripwire (W12). Subset mean-of-week-means preserves full-run levels and rank order — gpt-5.5 **0.854** (runs 0.900 / 0.817 / 0.800 / 0.900) vs full 0.865; opus 0.75 vs 0.819; luna 0.55 vs 0.681; terra 0.20 vs 0.388. Noise granularity: one target flip in a 3-target week moves the subset mean 0.083; treat ±0.05–0.10 as the subset band. Micro-screen `W04 W12` (8 targets; gpt-5.5 pooled 0.85, runs 0.90/0.90/0.70/0.90) is allowed for rapid iteration only. The old strong-week screen (`W06 W10 W11 W12`) is deprecated — it flattered the lean-overhaul family twice.
- **Round protocol change:** every variant screens on **gpt-5.5 medium** (promotion target) and **gpt-5.6-sol medium** (same cost, generally more capable — the explicitness diagnostic; no Anthropic drafters in the matrix for now). Sol has no numbers yet, so the round's first run is a sol-medium baseline on unmodified `exp/critic-2`. If a winning variant brings sol to parity, screen terra/luna (cheaper) on it. Promotion is unchanged: full 9-week gpt-5.5 replicates vs 0.865.

### round base pinned: exp/explicit-base (addendum to the round kickoff) — 2026-07-12

- **What:** The structural-explicitness round runs **without the critic**, per Ryan. The no-critic ablation edit was committed to its branch (`90e23c0` on `exp/critic-2-no-critic` — it had sat uncommitted in that worktree), and **`exp/explicit-base`** = that + the env-override config commit (`707d569`); worktree `.worktrees/explicit-base`, tests green, critic verified absent from the built prompt and tool list.
- **Why critic-less:** the critic rescues exactly the omissions the rewrite tries to prevent in the first draft, so it masks draft-formation deltas between variants; critic-less runs measure the rewrite directly and cost less. The promotion bar stays critic-2's 0.865. If a winning rewrite lands close-but-below, re-adding the critic on top is an explicit composition experiment. Known costs of removal: the floor (0.765 vs 0.806) and the audience effect.
- **No-critic subset baseline (`W04 W10 W12 W13`):** pooled **0.767** (r3 0.817, r4 0.717); W12-03 fails in both runs — the critic's reproducible save, visible in the subset as designed. Canonical rescores: `critic-2-no-critic-r3-rs-0710f` and `critic-2-no-critic-r4-rs-0710f-323bc5db`; the unsuffixed `critic-2-no-critic-r4-rs-0710f` (0.788, attempted 0.964) is a stale duplicate that disagrees with the ledger's pinned 0.765/0.919 — do not use it.

### explicit-base-sol baseline (gpt-5.6-sol medium on the unmodified round base) — 2026-07-12

- **What:** The round's first mandated run: sol-medium reference numbers on unmodified `exp/explicit-base`, so variant screens have a sol baseline. Generated in `.worktrees/explicit-base` via env override (`OPINION_AGENT_MODEL=openai:gpt-5.6-sol`), rescored from main under `2026-07-10-coverage-concepts` (`explicit-base-sol-r1-rs-0710f` subset, `explicit-base-sol-r2-rs-0710f` full 9 weeks). Zero agent/tool errors in both runs; per-week judge coverage verified.
- **Scores:** Subset `W04 W10 W12 W13`: opinion_quality **0.65** (W04 0.80, W10 0.33, W12 0.67, W13 0.80), attempted 1.000, recall 0.975, precision 0.739, brevity 0.723. Full 9 weeks: opinion_quality **0.7125**, attempted **1.000**, recall 0.958, precision 0.518, brevity 0.783.
- **Read:** Sol lags gpt-5.5 on the same prompt (subset 0.65 vs 0.767; full 0.7125 vs 0.806) but with a failure profile unlike every other non-gpt-5.5 model: **no refusals and no routing misses** — attempted is a perfect 1.000 in both runs, and sol converts W13-05 (the target gpt-5.5 has essentially never matched). The entire gap is concept-coverage fails on the known drop classes: W04-02 one-way ratchet, W04-04, W05-01, W08-04, W10-02 Price's Law, W10-03, W11-02, W12-03, W13-02. Sol writes shorter (brevity 0.72–0.78) and cites tighter (subset precision 0.739).
- **Implication for the round:** sol's gap is pure draft-fidelity implicitness — exactly the class the explicitness rewrite targets — so it is a clean diagnostic model. The refusal/eligibility failure mode (terra/opus) does not apply to sol.

### explicit-duty (round variant 1: explicit proposal duty + eligibility threshold)

- **Hypothesis:** STATUS explicitness items 1–2: replace the buried "Return Telegram messages for any conceptual opinion changes..." sentence with an explicit proposal-coverage contract — one proposal per argued cluster, zero-proposal weeks almost never legitimate, skipping only for named disqualifiers (reference material / already fully covered), familiarity and personal register explicitly not disqualifiers, borderline → propose. Targets the terra/opus refusal mode and the lean-overhaul-2 single-point-of-failure duty sentence.
- **Changed:** `prompts.py` `EVIDENCE_AND_WORKFLOW_INSTRUCTIONS` only — the one duty sentence expanded into the contract paragraph (net +5 lines). Branch `exp/explicit-duty` (`3934a0a`) from `exp/explicit-base`. Leakage scan passed.
- **Scores (subset `W04 W10 W12 W13`, rescored `-rs-0710f`):**
  - **gpt-5.5 medium:** pooled **0.742** (r1 0.633, r2 0.850; spread 0.217) vs base pool 0.778 (n=3) — a wash within the subset band. attempted 0.950 both runs, recall 1.000, precision 0.645 (≈ base subset 0.676), brevity 0.737. W13-05 unmatched 0/2; W04-01 0/2, W04-05 0/2.
  - **gpt-5.6-sol medium:** **0.950** (r1) vs sol base 0.65 — **+0.30, the largest single-model screen jump of any variant**. Every sol-base failure flipped to pass (W04-02, W10-02, W10-03, W12-03, W13-02); only W13-04 newly failed. attempted 1.000, recall 1.000, precision 0.661, brevity 0.734. r2 confirmation run in flight.
- **Verdict:** Kept as the round's working base (not a promotion — promotion needs full-run gpt-5.5 replicates). Dual-model read: holds gpt-5.5 within noise while closing sol's entire implicitness gap and then some — exactly the "explicitness generalizes" signature the round is hunting.
- **Learned:** (a) Sol's concept-drop failure mode responds to a *coverage/duty* contract even though the failures looked like fidelity drops — stating the deliverable explicitly appears to change how completely sol drafts, not just whether it proposes (audience-effect-like). (b) On gpt-5.5, r1's trace showed W04-01 answered with a revision proposal centered on the existing opinion's text, dropping the new evidence's concepts — the duty sentence's "or a revision of an existing one" nudges revision routing without a fidelity bar for revisions; variant 2 targets exactly that. (c) The base insurance run (`critic-2-no-critic-subset-r5-rs-0710f`, 0.800, W04 0.40 with W04-01 unmatched) showed the no-critic base is itself noisy on W04 — attribute W04 single-run flips to the base lottery, not the variant. gpt-5.5 base subset pool is now n=3: **0.778** (0.817 / 0.717 / 0.800).

### explicit-duty — sol confirmation addendum

- **What:** `explicit-duty-sol-r2` (subset) to test whether sol's 0.950 was a single-draw fluke.
- **Scores:** **0.817** → sol pooled **0.883** (0.950 / 0.817, spread 0.133) vs sol base 0.65; attempted 1.000 and recall 1.000 in both runs; precision 0.661 in both (zero spread); brevity 0.745. r2 fails: W04-04, W12-03, W13-05 — the same flaky classes gpt-5.5 churns on, not a new mechanism.
- **Read:** The sol gain is confirmed (+0.23 pooled, ~2–4× the subset band). Sol on explicit-duty now **outscores gpt-5.5 on the same prompt** (0.883 vs 0.742 pooled) and clears critic-2's gpt-5.5 subset baseline (0.854) without a critic. The explicit duty contract is what sol was missing; gpt-5.5 neither gains nor loses from it. One generation error occurred in r2 (single tool error; coverage and judging complete — verified).

### explicit-routing (round variant 2: drafter-side update routing + revision fidelity)

- **Hypothesis:** STATUS explicitness item 4 plus the W04-01 mechanism from explicit-duty's traces: before drafting, search OPINIONS.md for the cluster's named anchors and route hits as revisions; hold revisions to the same fidelity bar as new opinions (the revised text must carry the new evidence's load-bearing concepts, not just extend the existing claim).
- **Changed:** `prompts.py` only — the OPINIONS.md read sentence became the pre-draft anchor-search routing rule + revision fidelity bar. Branch `exp/explicit-routing` (`5936ea9`) from `exp/explicit-duty`. Leakage scan passed.
- **Scores (rescored `-rs-0710f`):**
  - **Subset:** gpt-5.5 pooled **0.800** (r1 0.900, r2 0.700) vs base pool 0.778; sol **0.950** (r1; only W04-02 failed). Subset precision 0.65–0.66 both models.
  - **Full 9 weeks (single runs each):** gpt-5.5 **0.950** (fails only W13-04 + W13-05-unmatched; all of W04 passed) and sol **0.950** (fails only W13-02, W13-04) — the two highest full-run `opinion_quality` numbers ever recorded (prior best 0.919), both clearing the with-critic 0.865 bar, without a critic, on both screen models. attempted 0.975/1.000, recall 1.000/0.979. Per-week judge coverage verified on both (reported means match verdict-implied means exactly).
  - **The catch: evidence_precision 0.460 (gpt-5.5) / 0.468 (sol) — below the 0.50 floor** (base full-run ~0.503–0.520). brevity 0.669/0.619 (longest of the round).
- **Precision autopsy (per-week Braintrust fetch + run-DB citation counts):** the loss concentrates in **W05 (0.27 vs 0.33–0.38 base; leaked 14–16 vs 9–10) and W11 (0.45–0.47 vs 0.52–0.61; leaked 17–18 vs 9–14)** — big multi-cluster weeks outside the screen subset, which is why screens (precision 0.65+) never saw it. W07 is 0.00-with-one-proposal in every run including base — a constant, not variant damage. Proposal *counts* are flat vs base (74 vs 73 messages); what grew is **citations per proposal** (W05: 22 distinct cited rows vs 18; several proposals citing 3–4 rows vs base's 1–2). The duty contract's "each cluster of selected rows... one proposal" bundles neighboring rows into the citation list; rows whose content the final wording doesn't carry are pure leaks.
- **Verdict:** Not promotable as-is (precision floor), but the round's central result so far: **explicit duty + routing contracts lift both models to 0.95 full-run quality**. The fix is a citation-scope contract (cite only rows whose content the opinion carries), not less coverage.
- **Learned:** (a) The quality mechanism and the precision regression are the same behavior seen from two sides — fuller clusters per proposal. (b) The screen subset is blind to citation-bundling precision loss; any variant touching clustering/citation needs a **W05 W11 precision probe** (~25% full-run cost) before full validation. (c) W13-05 matched for the first time on gpt-5.5 (r2, concept-fail rather than unmatched) — the anchor-search rule reaches it but doesn't yet carry the prior opinion's concepts. (d) sol on this lineage never fails W04 and converts W13-05 reliably; its only repeat offenders are W13-02/W13-04.

### explicit-reads (round variant 3: deterministic bounded-read rule + cluster anchoring)

- **Hypothesis:** STATUS explicitness item 3 via `eval/generation_determinism.md`'s two named variance engines: (1) replace the six-bullet "visibly signals missing context" read-trigger judgment list with a rule — every proposal-bound cluster gets a bounded read around each cited highlight, every time (audit-NO targets pass only on runs that happen to read the body; make the read unconditional); (2) re-anchor fidelity on the claim the cluster's selected evidence argues instead of "the claim the source actually centers" (the wrong-center engine).
- **Changed:** `prompts.py` — read-policy paragraphs (incl. the trigger list) replaced by the deterministic rule (net −10 lines); `RULES.md` — the fidelity anchor line re-scoped to the cluster. Branch `exp/explicit-reads` (`30bc57e`) from `exp/explicit-routing`. Leakage scan passed.
- **Scores (subset, rescored `-rs-0710f`, single run per model):** gpt-5.5 **0.817** (fails W04-02, W12-03, W13-05-unmatched) vs base pool 0.778 / explicit-routing pooled 0.800; sol **0.950** (fails only W13-05). Precision 0.646 / 0.676, brevity 0.669 / 0.656. attempted 0.95 both.
- **Cost note:** the unconditional reads roughly double run cost (~$2.07 for the 4-week gpt-5.5 subset vs ~$1 prior) — more source reading per proposal.
- **Verdict:** Dual-model hold with a simplification (the judgment-call trigger list is gone). Kept as the stacking tip; the W05/W11 precision leak it inherits from the duty contract is variant 4's job (`explicit-cite`).
- **Learned:** The trigger list was not load-bearing — replacing conditional-read judgment with an unconditional rule holds quality on both models, consistent with the determinism doc's claim that read-variance (not read-volume) was the engine. Both models' only shared residual on the subset is W13-05 (gpt-5.5 unmatched, sol concept-fail in this pair).

### explicit-cite (round variant 4: citation-scope contract)

- **Hypothesis:** Fix explicit-routing's precision-floor violation without touching coverage: cite exactly the rows whose content the final opinion carries (stance / mechanism / named specifics / bounds); a neighboring row swept into the cluster but unused by the wording is not cited, and an uncited row still triages normally.
- **Changed:** `prompts.py` — four lines added to the duty paragraph. Branch `exp/explicit-cite` (`11aa22d`) from `exp/explicit-reads`. Leakage scan passed.
- **Scores (rescored `-rs-0710f`):** W05 W11 precision probe: **W11 0.517 (recovered to base level)**, W05 0.273 (unmoved); probe quality 0.875 with attempted/recall 1.000 (only W05-02's payoff flake failed). Subset: gpt-5.5 **0.900** (fails W04-01, W13-05-unmatched; precision 0.661), sol **0.817** (fails W04-05, W12-03, W13-04; precision **0.694**, best of the round).
- **Verdict:** Half the precision fix: it ends citation *bundling* (W11, and per-message citation counts back to 1–3) but not W05's leak, which the run DB shows is **whole extra proposals** — ~13 W05 proposals vs base's ~11, leaked IDs spread 1–2 per message across 12 messages, including 4 reader-note-seeded proposals GT does not convert.
- **Learned:** The routing-variant precision loss was two mechanisms, not one: citation bundling (fixed here) and borderline-cluster proposal spam concentrated in W05 (not fixed). Quality is undamaged by citation narrowing — recall stayed 1.000.

### explicit-selective (round variant 5: drop the borderline-propose license)

- **Hypothesis:** W05's residual leak comes from the duty contract's "when eligibility is borderline, propose it and let Ryan decide" — deleting that sentence (keeping the duty, the named disqualifiers, and the not-a-disqualifier list) should cut the extra W05 proposals.
- **Changed:** `prompts.py` — that one sentence removed. Branch `exp/explicit-selective` (`8ba4c99`) from `exp/explicit-cite`. Leakage scan passed.
- **Scores (probe only, rescored):** W05 precision **0.300** (vs cite 0.273, base 0.33–0.38; leaked 14 vs 16), W11 0.500. Probe quality 0.875 (same single W05-02 miss), attempted/recall 1.000.
- **Verdict:** Marginal — the borderline license was not the W05 driver either. Reading the leaked rows attributed most of the persistent leak to **reader-note-seeded proposals**: notes that merely react/associate/bookmark ("reminds me of X", product-mechanic explainers) become their own proposals because the notes-are-stronger-evidence rule reads as an eligibility boost. That is variant 6 (`explicit-notes`): notes strengthen an argued cluster's endorsability; they do not seed proposals on their own.
- **Learned:** Full-run precision projection for this lineage is ~0.48–0.50 — riding the floor; the note-seeding rule is the remaining identified lever before full validation.

### explicit-notes (round variant 6: notes strengthen clusters, don't seed proposals)

- **Hypothesis:** From explicit-selective's leak autopsy: W05's residual precision leak is reader-note-seeded proposals — the RULES.md "notes are stronger evidence" bullet reads as an eligibility boost, so reacting/associating/bookmarking notes become their own proposals. Rewrite the bullet: a note raises an argued cluster's endorsability weight but is not itself an argued stance and does not seed a proposal.
- **Changed:** `RULES.md` — the notes bullet rewritten. Branch `exp/explicit-notes` (`616598b`) from `exp/explicit-selective`. Leakage scan passed.
- **Scores (rescored `-rs-0710f`):** W05 note-seeded leaked proposals halved (4 → 2); W05 precision still ~0.27–0.30 (residual ~14 leaked rows are argued clusters GT declined — judged irreducible without leaking GT knowledge; a structural ~−0.03 full-run precision cost of the coverage duty). Subset: gpt-5.5 **0.900** (precision 0.646; W13-05 matched-but-concept-failed), sol **0.817** (precision 0.694, attempted/recall 1.000). Full 9-week gpt-5.5 (r2, single run): **0.877**, attempted 1.000, recall 0.979, precision 0.466.
- **Verdict:** Validation battery aborted mid-run by design change: Ryan deprioritized precision entirely (2026-07-12: rejecting a bad proposal is cheap, so precision is not an important operational metric, and variants 4–6's precision probes were retroactively low-value). The stack still holds quality (0.877 full vs 0.865 bar) but the round pivoted to consistency of the quality peak, `explicit-routing`.
- **Learned:** The note-seeding mechanism was real (halved on rewrite) but the remaining W05 leak is GT-declined argued clusters, not fixable prompt-side. Precision is no longer a gating metric per Ryan.

### explicit-routing consistency round (sol n=3, terra n=2, full 9-week runs)

- **Question (Ryan):** Is the 0.95 variant consistent? Run one more sol full run and a first terra run; no gpt-5.5 re-runs. (Then extended to sol r3 + terra r2.)
- **Scores (full 9 weeks, rescored `-rs-0710f`; judge coverage verified 33/33 on every run):**
  - **sol:** r1 **0.950** (`explicit-routing-sol-r2`), r2 **0.796**, r3 **0.950** → **pooled 0.899 (n=3)**. attempted 1.000 all three; recall 0.98–1.00; precision 0.468/0.474/0.472.
  - **terra:** r1 **0.894**, r2 **0.806** → **pooled 0.850 (n=2)**. attempted 0.975/0.944.
- **sol r2 (0.796) autopsy:** its five extra failures beyond the chronic W13-02/W13-04 (W04-04, W05-01, W08-04, W11-04, W12-03) are ALL matched, stance-agreeing, correctly-cited proposals that dropped one concept in drafting — an enumeration member flipped sides (W11-04: brand listed as an asset to build instead of a thing not to optimize for), a co-equal half dropped (W12-03: kept keep-human, dropped hand-agents-the-routine), or a near-miss paraphrase judged strictly (W04-04, W05-01). No refusal/routing/read variance: same clusters, same matches across runs. This is exactly the omission class the critic used to repair; with 33 targets a single run has σ≈0.06 from per-target flips, so 0.95→0.796 is a ~1.5–2σ draw with a fatter lower tail because nothing clips drafting-compression drops.
- **terra citation defect:** terra omits evidence IDs from proposal messages — writes "Article title — null" and quotes the generated document summary instead of selected highlight rows (5/9 weeks in r1, 9/9 in r2). recall/precision are meaningless for terra (0.375/0.807 r1, 0.000/1.000 r2); quality judging is unaffected (concept-based). Operationally this would break OPINIONS_SOURCES.jsonl attribution; terra needs a copy-IDs-verbatim nudge before more terra runs say anything new.
- **Verdict:** sol clears the with-critic bar pooled (0.899 > 0.865, 2 of 3 runs at exactly 0.950; misses in the low run are draw noise, not a mode). terra is a step behind (0.850) with a real formatting defect. gpt-5.5 full remains n=1 (0.950) by Ryan's no-re-run directive.
- **Learned:** (a) Run-to-run variance in this critic-less lineage is drafting compression — the both-halves/full-enumeration fidelity classes — concentrated randomly, not week-anchored. (b) The cheapest floor-raiser candidate is a drafter self-check (re-read each draft against cited evidence for dropped halves/enumeration members before sending) — critic-shaped, no second model call. Proposed as variant 7, awaiting Ryan.

### explicit-critic (round variant 7: subagent critic with a typed evidence-fetch tool)

- **Hypothesis:** Re-add a critic on top of `exp/explicit-routing` to clip the drafting-compression tail that produced sol's 0.796 draw — but structurally different from critic-2: a **thinharness subagent** (not a Braintrust-proxy tool call), one call per proposal issued in parallel, whose only tool is a typed `get_evidence(evidence_ids)` fetch against the week's selected highlights. The drafter passes the exact draft text + cited evidence IDs as free text; the critic extracts the IDs and fetches the rows itself. Critic prompt rewritten from scratch on the proven critic-2 spine (omission-only, cited-cluster-scoped, default READY when unsure) plus two additions from the 0.796 autopsy: enumeration-member **polarity** (a named specific on the wrong side of the stance is missing, not reworded) and the **adjacent-move boundary** (a generically similar move is not the same concept). Critic model inherits the drafter (`model=None`).
- **Changed:** Branch `exp/explicit-critic` from `exp/explicit-routing` (`5936ea9`); worktree `.worktrees/explicit-critic`. `prompts.py` — TOOL_INSTRUCTIONS critic bullet (one call per proposal, parallel, fold REVISE by tightening never deleting, no second call) + `CRITIC_SYSTEM_PROMPT`; `agent.py` — `build_evidence_fetch_tool`, `build_critic_subagent`, `subagent` added to `builtin_tools`; thinharness pinned to v0.5.3. Leakage scan passed. **Infra shift (all future runs):** drafter + critic LLM calls route through a dedicated codex-backend proxy on port 8113 (ChatGPT-subscription OAuth instead of API credits; patched copy with `CODEX_CHAINS_MAX=500` — the stock LRU of 20 evicted chains under eval concurrency and killed a week). Judge calls unaffected (scorers pin `base_url` to the Braintrust proxy).
- **Build lessons:** the `subagent` gateway must be listed in `builtin_tools` or configured subagents are silently unreachable (first smoke ran critic-less); the free-text task interface let the drafter batch 7 drafts into one critic call until the prompt bullet mandated per-proposal parallel calls (smoke-3 verified 5 proposals → 5 calls).
- **Scores (sol medium, rescored `-rs-0710f`; judge coverage verified 33/33 on every run):** subset **0.950** (ties the sol anchor; only W13-02 fails; 29 critic calls, 34% REVISE). Full 9 weeks: r1 **0.883**, r3 **0.925**, r4 **0.846** → **pooled 0.885 (n=3), min 0.846, spread 0.079** vs anchor explicit-routing sol 0.950/0.796/0.950 (pooled 0.899, min 0.796, spread 0.154). attempted 0.975–1.000, recall 0.948–0.979, precision 0.470–0.484, brevity 0.606–0.659. (r2 voided: 3 upstream OpenAI `server_error`/transport failures killed week rows; replaced by r4, which ran clean.)
- **Did the critic help? (r1 trace audit, 79 calls / 25 REVISE):** yes, measurably. At least four REVISEs injected the judged target's load-bearing concept into the sent wording and the target passed: W04-01 (the historically dead "prompting is not the newly scarce skill" + idea-quality hierarchy), W10-01 (already-outsourced work: customers already accept a third party owning execution), W10-03 (ask the model what concepts it used / what to read), W13-01 (compression-vs-discovery-speed tradeoff). The unresolved-ID guardrail also caught two bad citation sets (W06, W12) and forced fixes.
- **Counterfactual replay (0.796 anchor run's five misses through the real critic):** saves ~1 of 5 — W04-04 REVISE demands exactly the missing concept (tell your manager + ask what must be true); W08-04 and W11-04 get correct READYs because the dropped half was **never cited** (it is not in any cited row); W05-01 and W12-03 get REVISEs pointed at the *wrong* half. Correction to the round's earlier theory: the 0.796 compression drops were mostly **not** within-cited-evidence omissions — the drafter dropped the concept and its citation together, so a cited-scope critic sees a coherent narrower claim. The critic would have lifted 0.796 to ~0.82, not 0.95.
- **The residual failure signature (3× now):** W12-03's "hand agents the routine work" half failed identically in the 0.796 anchor, critic r1, and critic r4 — the drafter grafts the in-the-loop half onto a taste/canonical opinion, cites only the one in-the-loop highlight, critic correctly READYs. The missing half lives **only in the parent document's summary** (no selected highlight carries it); W11-04's "not pay/title/brand" negative list likewise lives only in the article summary/body. **Variant-8 candidate:** `get_evidence` also returns the parent document's summary for each cited row (one paragraph per cited document, deterministic, no cross-document scope) — this would have put the missing half in front of the critic in all three W12-03 failures and both W11-04 failures.
- **Known defect (Revise-type proposals):** a revised opinion inherits claims backed by prior-week sources the critic cannot resolve, so the critic flags the inherited half as unsupported (both wrong-half REVISEs in the replay; r1's W12 unresolved-ID REVISE) — REVISE noise that could shrink good revisions. If kept, the critic prompt should scope revisions to the newly added claims.
- **Verdict:** Above the 0.865 bar pooled (0.885) with the floor raised 0.796 → 0.846 and the spread halved — the critic does the tail-clipping job it was re-added for, at parity mean. Not yet a peak-matcher (no 0.950 full run; anchor had two). Promotion vs variant-8 iteration (doc-summary fetch + revision scoping) is Ryan's call.
- **Learned:** (a) The critic's value splits cleanly by where the loss happens: it repairs lossy *drafting* over well-cited clusters (four r1 saves) and cannot see lossy *citation* (concept + citation dropped together) — the two look identical in aggregate scores and need trace-level attribution to distinguish. (b) The free-text subagent task interface needs cadence rules in the drafter prompt that the old typed schema enforced structurally. (c) ChatGPT-backend upstream 5xx errors are a new transient run-killer under concurrency; check `provider request failed` counts before rescoring and void partial runs.

### explicit-critic-docscope (round variant 8: critic fetch widened to same-document context)

- **Hypothesis (Ryan-directed):** Variant 7's residual misses were outside the critic's sight — the dropped half lived in the parent document's summary (W12-03 3×, W11-04) or an uncited same-document selected highlight (W08-04), never in the cited rows. Widen `get_evidence` to return, per cited row's document, the document summary plus the other selected rows from that document (labeled "not cited by this draft"). No full-document bodies. Everything else — task format, per-proposal parallel cadence, drafter bullet — unchanged.
- **Changed:** Branch `exp/explicit-critic-docscope` (`a0c2f3b`, `+1` prompt commit) from `exp/explicit-critic`; worktree `.worktrees/explicit-critic-docscope`. `agent.py` — fetch groups cited rows by document, appends document summaries and uncited same-document rows from `selected-documents.jsonl`/`selected-highlights.jsonl`. `prompts.py` critic prompt, three iterations forced by smoke/replay evidence (the first version failed its own design case — the critic READY'd with the agentic-loop summary in view, and replays showed REVISEs pointed at *support-checking* the draft's inherited claims instead of omissions): (1) "complete the argument" framing for same-document context; (2) whole-claim check gains the complementary-split case (more of one thing, less of / handing off another) and the note that the dropped half may live only in same-document context; (3) bullet-direction rule — every REVISE bullet points from visible evidence to draft; a draft claim lacking support is never a bullet; claims backed only by unresolved IDs are out-of-scope content, not "unverified" flags. Leakage scans passed on both commits.
- **Verified mechanism:** replay of the W12-03 exchange flagged the delegation half 2/3 after the rewrite (0/3 before); the live smoke then produced the designed behavior end-to-end — clean one-bullet REVISE ("add the complementary shift to human-on-the-loop supervision for routine agent tasks"), drafter folded it, W12-03 passed.
- **Scores (sol medium, rescored from main `-rs-0710f`; coverage verified 33/33 every run):** subset **0.900** (14/16; fails W04-01, W04-04; W13 5/5 including both chronic misses). Full 9 weeks: r2 **0.919** (30/33), r3 **0.877** (29/33), r4 **0.894** (29/33) → **pooled 0.897, 88/99, min 0.877, spread 0.042**. attempted 1.000 all three; recall 0.979; precision 0.475–0.489; brevity 0.60–0.66. Critic engagement: 79–81 calls/run, 45–46% REVISE (vs 32–34% cited-scope). (r1 voided: 3 upstream 5xx-killed weeks; note one tainted rescore name `…full-r2-rs-0710f` was re-run from main as `…full-r2-rs-0710f-152bca0c` — worktree judge code differs from main; only the `-152bca0c` one is canonical.)
- **Raw-count comparison (x/33 per full run, sol):** explicit-routing 31/26/31 (88/99, min 26); explicit-critic 29/30/28 (87/99, min 28); docscope **30/29/29 (88/99, min 29)**. Pooled means are statistically identical across all three; what moved is the tail and the target-level fix: **W12-03 passed in all 4 docscope runs** (subset + 3 fulls) after failing 3 of the prior 6 sol runs, and the fix is causally traced (critic bullet → folded draft → pass). W11-04 remains flippy (failed r2 — the critic READY'd even with the summary's negative list in view, because the cited highlight itself lists brand as an asset; the same named specific plays opposite roles in highlight vs summary — judged the residual class, not worth bespoke rules). Chronic W13-02/W13-04 passed in the subset and r2 but returned in r3/r4.
- **Verdict:** Best floor and tightest spread of any sol variant (min 0.877 vs 0.846 cited-scope vs 0.796 no-critic) at a pooled mean indistinguishable from the no-critic peak line, with the designed failure class demonstrably closed. Costs: higher REVISE rate (~46%) without quality damage, brevity lowest of the lineage (~0.60), n=3 so the tail claim is mechanical (traced saves) more than statistical. Promotion vs further iteration is Ryan's call.
- **Learned:** (a) Context widening alone does nothing — the critic ignored the new material until the prompt reframed its use ("does it complete the argument the cited rows started?"); tool surface and prompt frame have to move together. (b) The support-checking failure mode (auditing draft claims against retrievable evidence) is the subagent critic's natural attractor whenever unresolved IDs are present; the bullet-direction rule suppressed it in live runs. (c) A named specific playing opposite roles in different evidence granularities (W11-04's brand) defeats an omission critic honestly — that class needs upstream citation/clustering work, not critic scope.

### explicit-critic-docscope replicates r5–r6 (floor/peak check, Ryan-directed)

- **Question (Ryan):** are the no-critic 0.950 peaks a feature of `exp/explicit-routing` or random chance — and does the docscope floor hold under more draws? Two more full 9-week sol runs of the unchanged docscope build (`.worktrees/explicit-critic-docscope`, same commits, proxy 8113).
- **Scores (rescored from main `-rs-0710f`; coverage verified per week):** r5 **0.894** (29/33; misses W13-02, W13-04, W11-04, W05-01 — all previously known residuals, no new failure modes), r6 **1.000** (**33/33 — first perfect full run of any variant**, including both chronic W13 targets and W11-04). Both generation runs had zero provider failures. W12-03 passed both, now **6/6 docscope runs**.
- **Updated docscope distribution (n=5):** 0.919 / 0.877 / 0.894 / 0.894 / 1.000 → **pooled 0.917, raw 150/165, min 0.877**. vs explicit-routing n=3: 0.950/0.796/0.950, pooled 0.899, 88/99, min 0.796.
- **Verdict:** the peak question is answered — high peaks are not exclusive to the no-critic variant; docscope just posted the lineage's only 1.000 while keeping the best floor (0.877). At n=5 vs n=3 the pooled gap (0.917 vs 0.899) is still within noise, but docscope now leads on mean, floor, and peak simultaneously.
- **Note:** two explicit-routing proxy replicates launched earlier the same evening (r4/r5) were both killed by upstream provider failures (6 and 3 respectively) and voided before Ryan redirected the replicate budget to docscope; no clean explicit-routing-on-proxy datapoint exists yet.

### Reporting convention change: target-weighted quality is primary (2026-07-21, Ryan-directed)

- The Braintrust experiment headline for `opinion_quality` is a mean of week means (one eval case per week), so a 3-target week weighs the same as a 5-target week and single-run headlines drift from the raw pass fraction by up to ~0.015 (e.g. docscope r5: 0.894 headline vs 29/33 = 0.879 raw). Ryan's call: the **target-weighted raw fraction (x/33 per full run)** is the primary quality number going forward.
- The Braintrust SDK caps per-case scores at 1.0 and averages cases equally, so the UI headline cannot be made target-weighted while cases are weeks (a clean 5-target week would need a 1.21 score). Implemented instead: `summarize_target_weighted_quality` in `evals/runner.py` — both `run_opinion_eval` and `rescore_opinion_eval` now print `opinion_quality (target-weighted): x/y = 0.xxxx` after the Braintrust summary. No scorer changed, no SCORING_VERSION bump (existing scores are untouched); unit-tested in `tests/test_evals.py`.
- Pooled raw standings under the new convention: explicit-routing 88/99 = 0.889, explicit-critic 87/99 = 0.879, docscope 150/165 = 0.909. Ordering unchanged from headline pooled; docscope's lead is slightly cleaner.

### explicit-critic-docscope replicate r7 (floor confirmation, Ryan-directed)

- **Question (Ryan):** one more full run to confirm no further/lower dip below the 29/33 floor.
- **Score (rescored from main `-rs-0710f`, coverage verified, zero provider failures):** **28/33 = 0.848** (headline 0.852) — a new docscope floor, one target below the previous worst. Misses: W13-02 (chronic), W11-02 (known flippy, now 3 of 4 critic-lineage fails), W11-04 (two-role residual), plus W10-02 and W08-04 flips. W13-04 and W12-03 passed (W12-03 now 7/7 docscope runs). Notably W08-04 is the uncited-same-doc-highlight class the docscope widening was designed to expose — it still flipped.
- **Updated docscope distribution (n=6):** 30/29/29/29/33/28 → **pooled 178/198 = 0.899, min 28/33 = 0.848, max 33/33**. vs explicit-routing 88/99 = 0.889 (min 26/33), explicit-critic 87/99 = 0.879 (min 28/33).
- **Verdict:** the floor story softens with n — docscope's min now ties explicit-critic's (28/33) rather than beating it, though it stays 2 targets above the no-critic 26/33 draw. Mean lead holds (0.899 vs 0.889 vs 0.879) and it still owns the only perfect run. The n=3 "tightest spread" claim was itself partly a small-n artifact — consistent with σ≈0.06 single-run noise dominating everything except the traced W12-03 fix.

### brevity round: scaffolding removal (variants brevity-scope 1–3, Ryan-directed 2026-08-09)

- **Motivation:** docscope proposals run **1.81x golden length** on the perfect r6 full run (59w vs 34w; 406 vs 219 chars), so only **3/33 fit in a tweet** against 30/33 of the goldens. Trace analysis of all 33 r6 opinions attributed the excess not to concept coverage — corr(required-concept count, length ratio) = **-0.04** — but to framing: a prepended premise motivating the claim, an appended justification/restatement, and repetition of other opinions' claims as setup. Cross-proposal check: 3 r6 proposals carry another target's golden claim near-verbatim (W11-03 shares 10 five-grams with W05-02's golden), while **zero** golden pairs share any five-gram. Counter-evidence for naive compression: pooled over 6 docscope full runs, within the 12 targets that both passed and failed, the passing attempt was longer in **12/12** (mean +15.5 words) — shorter attempts are the ones that drop a concept.
- **Lever:** RULES.md only, replacing the closing "Default to the faithful, fuller opinion" license in Opinion Writing Fidelity — that sentence was the explicit bloat permission. All three variants are subset screens (`W04 W10 W12 W13`, 16 targets) on sol medium via cproxy 8113, rescored from main as `-rs-0710f`. Leakage scans passed on all three.
- **brevity-scope (v1):** "keep the claim and only the claim" — cut setup, closing justification/restatement, and borrowed framing. **13/16 and 15/16 → 28/32 = 0.875 at 1.50x golden** (3 and 4 of 16 tweetable). Quality equal to the docscope baseline (14/16 at 1.84x), length down 18%. Failure mechanism: the rule cuts but does not rank — W10-02 shrank 70w→42w by dropping the **Price's Law / √n** named anchor, W04-05 dropped the core claim while keeping a how-to procedure, and W12-03 (7/7 in docscope) kept a textbook prepended premise and dropped the routine-work half.
- **brevity-scope-2 (v2):** adds procedures to the cut list, ranks the fidelity ingredients above cutting ("the ingredients above are never what you shorten"), and grants explicit permission to stay long once framing is gone. **16/16 and 16/16 → 32/32 = 1.000 at 1.63x golden.** Beats the baseline on both axes.
- **brevity-scope-3 (v3):** v2 minus the leave-it-long permission, to isolate a variable v2 bundled. **14/16 = 0.875 at 1.50x** (n=1; a first run was voided for a W10 row with missing output and relaunched).
- **Verdict:** a consistent quality/length frontier, not noise in one direction. Both variants that reach 1.50x sit at 0.875; the only variant that stays at 1.63x reaches 1.000. The leave-it-long sentence is doing **quality** work, not length work — it licenses keeping the fidelity ingredients when they make an opinion long, and removing it restores the compression pressure that drops named anchors and claim halves. **brevity-scope-2 is the promotion candidate** (better than baseline on both axes); full 9-week replicates are still required, since 32/32 on a 16-target subset is a small-n figure and the baseline itself scored 14/16 there.
- **Open:** even the best runs fit only 4–5 of 16 opinions in a tweet vs 14/16 for the goldens. Closing that gap means compressing enumerations and named specifics, which is the bucket the 12/12 pass-length evidence says is load-bearing — a product decision, not a free prompt win.

### brevity-scope-2 full-run validation (2026-08-09) — subset result did not replicate

- **Runs:** two full 9-week sol runs of `exp/brevity-scope-2` (`.runs-full-r1`, `.runs-full-r2`, proxy 8113), zero provider failures, rescored from main as `-rs-0710f`.
- **Quality:** **28/33 and 28/33 → 56/66 = 0.848**, against docscope's 178/198 = 0.899 (n=6, distribution 30/29/29/29/33/28). Both runs landed exactly on docscope's observed floor rather than near its mean. At n=2 this cannot be separated from an unlucky pair (single-run σ ≈ 1.7 targets), but there is no evidence of the subset's quality gain.
- **Length:** **1.68x / 1.69x golden (55w)** vs the docscope full-run mean of **1.82x** (1.81/1.84/1.90/1.79/1.81/1.75) — a **7% reduction**. Tweet-fit went from a docscope mean of ~3/33 to **5/33**, against 30/33 for the goldens.
- **Verdict: the 16-target subset badly oversold this variant.** On the subset brevity-scope-2 read 32/32 vs a 14/16 baseline at 1.63x vs 1.84x; on the full set it reads 0.848 vs 0.899 at 1.68x vs 1.82x. The quality gain vanished entirely and the length gain shrank from 11% to 7%. This is a textbook case of the STATUS rule that subset screens are tripwires and never validation — the effect size on 16 targets was not merely noisy, it inverted.
- **Round conclusion:** the scaffolding diagnosis was correct (framing carries no required concept, and the worst offenders did shrink), but removing framing is worth only ~7% of full-run length because the bulk of the excess is fidelity-rule material — named anchors, full enumerations, de-compressed abstractions — that the rules deliberately require. Buying the remaining length means loosening rules that `lean-fidelity` already showed are load-bearing (0.446 when consolidated). Recommended instead: keep docscope, and derive tweet-length text from an approved opinion as a separate step rather than compressing the belief map to fit a character limit.

### production methodology selection (2026-08-11)

- Ryan selected `exp/explicit-critic-docscope` as the production methodology.
- Production uses `openai:gpt-5.6-sol` at medium effort and ThinHarness 0.6.0.
- Each proposal gets one omission-only critic call with cited rows and fixed same-document context.
- The deployment work rebuilds these changes against `main` without experiment run artifacts.

### operation-gated quality v2 and routing rounds (2026-08-20)

- **Eval change:** V1 remains unchanged. V2 copies the weekly eval and adds deterministic `operation_accuracy` plus `opinion_quality_v2`. A target passes V2 only when it passes the existing conceptual judge and uses the target's operation. Add targets require add proposals. Update targets require revision proposals that identify the canonical base opinion. V2 can rescore stored Braintrust generations.
- **Stored baseline:** The six clean production-method runs have conceptual quality **178/198 = 0.899**, operation accuracy **131/198 = 0.662**, and V2 quality **115/198 = 0.581**. The canonical r7 Braintrust rescore is `explicit-critic-docscope-sol-full-r7-v2-0820`: **28/33 conceptual, 22/33 operation, 18/33 V2**.
- **Round 1 — `v2-routing-threshold-r1`:** Raised the revision threshold, made shared topic or mechanism insufficient, and defaulted uncertain boundaries to add. **28/33 conceptual, 28/33 operation, 24/33 V2 = 0.727**. It produced 66 proposals: 55 adds and 11 revisions. This is the best round and the selected prompt state.
- **Round 2 — `v2-revision-is-replacement-r1`:** Allowed revision only when the old opinion would become inaccurate or misleading. **24/33 conceptual, 28/33 operation, 23/33 V2 = 0.697**. It produced 62 adds and no revisions. The rule overcorrected: it missed both update operations and skipped related-but-incomplete claim clusters, reducing recall and conceptual coverage.
- **Round 3 — `v2-routing-audit-r1`:** Restored broader revision eligibility and added a final internal routing audit. **22/33 conceptual, 27/33 operation, 21/33 V2 = 0.636**. It produced 73 proposals: 66 adds and 7 revisions. The audit did not improve routing and conceptual variance dominated the result.
- **Verdict:** Keep round 1 as the candidate. Its single-run V2 score is 14.6 points above the six-run stored baseline, but conceptual quality is 5.1 points below the stored baseline. Do not call it a production promotion from one draw. Replicate the unchanged round-1 prompt before deciding. Round 2 also shows why maximizing adds is not the objective: operation accuracy held while conceptual coverage and update routing degraded.
- **Target note:** Operation labels now directly control credit. Several add targets are close to seeded opinions, so target review remains useful before treating V2 as a durable optimization oracle. Do not change those labels inside a prompt experiment.
